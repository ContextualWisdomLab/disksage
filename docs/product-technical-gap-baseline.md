# DiskSage product and technical gap baseline

The canonical product outcomes, supported capabilities, non-goals, and safety invariants are defined in the [DiskSage PRD](PRD.md). This file is the current implementation and ownership snapshot; runtime receipts, exact filesystem evidence, released contracts, protected refs, and live GitHub governance remain authoritative. The prior 2026-08-22 detailed incident baseline is preserved without loss at [docs/archive/product-technical-gap-baseline-2026-08-22.md](archive/product-technical-gap-baseline-2026-08-22.md).

**Snapshot:** 2026-09-05 (Asia/Seoul)

**Protected main:** `6125310d9ea562c0ed36db7ab940a96ac9b32e53`

**Product boundary:** Windows/Linux/macOS local-first disk-space inventory and evidence-bound reclaim. Reversible deletion safety, filesystem classification/ontology, recovery, and platform adapters remain DiskSage domain truth. Optional cloud, identity, ontology, LLM, security, and architecture integrations consume released owner contracts through ports/ACLs and do not become mutation authority.

**Evidence rule:** queued, pending, skipped-required, failed, cancelled, stale, predecessor, model-only, or status-only evidence is non-passing. Exact current head and independently resolved live base are required for readiness claims.

## Current dependency and ownership map

| Lane | Exact head | Base / owner relation | Current status | Required next proof |
| --- | --- | --- | --- | --- |
| PR #264 — release artifact verifier | `022887a7a234e9cf5bfce5361584be324a9d29b2` | `main`; canonical combined #341/Test + release-verifier owner | Draft, mechanically mergeable. The stale `run_attempt` attestation/provenance assertions were repaired without changing production release semantics. Release is terminal-success; Test, Security, SAST, and CodeQL are still non-terminal on this exact head. | Obtain terminal-success applicable gates and live review/ruleset satisfaction on one unchanged head before Ready/merge. |
| PR #263 — cache-Trash fail-closed security | `b3b52fe1db8ed7e8e856e7c89f01080f38d96945` | stacked on exact #264; canonical issue #170 runtime owner | Draft, mechanically mergeable. Fresh #264→#263 is descendant-only with merge base exactly #264 and the semantic delta remains 13 cache/security files. | Establish #264 on protected lineage, obtain unchanged-head gates, then merge normally. Permanent cache-Trash deletion remains unavailable. |
| PR #315 — canonical product/public docs | resolve live on `codex/canonical-prd` | stacked on exact #263; docs/public-surface owner only | Draft. Non-force restack adopts current #263/#264 ancestry while retaining the canonical documentation/public surface. This file is the single current product-gap projection. | Re-resolve the branch after each docs commit, reacquire exact-head checks/reviews, and keep runtime authority with runtime owners. |
| PR #195 — Inventory privacy/async-state owner | `747f4c7baf38a2aa87c8267eff016a3ecb3233c0` | stacked on exact #264 after non-force restack | Draft, mechanically mergeable. Fresh #264→#195 is `behind_by=0`, merge base exactly #264, with exactly four Inventory semantic files. | Preserve the four-file Inventory boundary, obtain terminal exact-head gates/review, and do not reintroduce release/workflow ownership here. |
| PR #190 — Settings persistence/failure-feedback owner | `5d9396db8d381fb58483cb2e9e440b11c910f57c` | stacked on exact #264 | Draft, mechanically mergeable. The settings owner distinguishes first-run absence from unreadable/unsafe existing state and replaces persisted JSON through a complete same-directory temporary file. | Obtain unchanged-head Test/Release/security/review GREEN; remove/delegate legacy unregistered command adapters in their owner. Do not claim containing-directory power-loss durability without platform evidence. |
| PR #199 — Git worktree feedback/recovery UX | `a831a93fd7954d409ddbca17ccc09c9d4f924757` | stacked on exact #264; UI privacy/recovery feedback owner only | Draft, mechanically mergeable. The previous Release built all platform artifacts and failed only in the shared verifier; the branch now consumes #264 through non-force ancestry and retains exactly three semantic UI/privacy files. | Obtain unchanged-head Test/Release/security/review GREEN and integrated coverage. Keep Git retention, identity, approval, mutation, journal, and filesystem authority outside this UI owner. |
| PR #338 — contract-doc CI path filter | `9e236eb93e27f570ddbbe489a58e76b15911c0e8` | stacked on exact #264 | Draft, mechanically mergeable; semantic delta remains `.github/workflows/test.yml` plus `src/lib/testWorkflowPathFilterContract.test.ts`. | Prove supported ordered `paths` semantics on the unchanged exact head and satisfy current gates/review before parent-first integration. |
| PR #337 — exact-head coverage foundation | `dfc0737120449134a627033892bd85dae9badf93` | stacked on exact #338 | Draft, mechanically mergeable. Exact 100% thresholds remain unchanged; current owner carries command/cloud/adapted-iCloud/real-Git-worktree donor evidence while provider OAuth remains #339-owned. | Finish the #156 inheritance ledger, obtain terminal exact-head gates after #338, and prove non-vacuous integrated 100% owned-production coverage. |
| PR #339 — provider-OAuth domain authority | `a72a4fa6830e1ac6eb54a4b477d3e5b88000de87` | stacked on exact #264; connection-document/token/callback/credential-recovery owner | Draft, mechanically mergeable; 27 provider-OAuth domain source/test files remain the semantic owner delta. | Close pathname-ancestor replacement TOCTOU and post-rename directory-durability gaps with realistic platform filesystem evidence without moving shipped CLI/process behavior here. |
| PR #212 — provider-OAuth/iCloud operational CLI | `0e9137bcea740a03ed57547a633661f90d99c0a6` | stacked on exact #339; shipped CLI/process/platform-adapter owner | Draft, mechanically mergeable; 13 CLI/process files remain the semantic delta. | Establish #339 first, then prove current-head Test plus applicable Windows process/package/security gates without copying OAuth domain authority. |
| PR #202 — scan/navigation UI owner | `83607d3ec992c9b59e90c318f28364bbd5489c60` | stacked on exact #264; canonical `src/routes/+page.svelte` owner | Draft, mechanically mergeable; five page-owned semantic files remain. | Obtain terminal exact-head gates and preserve #202 as the only parent-page state owner before integrating #203. |
| PR #203 — TopFiles accessibility | `5d52d7a1e4594e541b96259f31b8d92ac038efb0` | stacked on exact #202; TopFiles component/accessibility owner | Draft, mechanically mergeable; semantic delta remains `TopFiles.svelte` plus its focused accessibility contract. | Issue #340 must provide real-browser normal/empty/loading/error, Tab/focus/scroll, fragment continuity, responsive overflow, and visible-focus evidence before the outstanding interaction finding is resolved. |
| PR #313 — exact-photo duplicate audit | `85575d442047c5e9fb36543b4ed56988d758ae6d` | stacked on exact #264; photo runtime/ADR owner, while #315 owns product-gap projection | Draft, mechanically mergeable; bounded exact-PNG audit remains read-only and its 13 photo-owned semantic files do not grant cleanup authority. Release is terminal-success; Test remains non-terminal. | Obtain unchanged-head Test/security/review GREEN. Keep cleanup unavailable until calibrated perceptual evidence, customer tie resolution, fresh execution preflight, and reversible Trash/quarantine + undo evidence exist. |
| PR #314 — native temporary-artifact reclaim | `840a92ebbaa486a65d352a656aa05fb198956fdd` | stacked on exact #264; native-temp admission/reclaim adapter owner | Draft, mechanically mergeable. The runtime lane now excludes `docs/product-technical-gap-baseline.md`; its 12 semantic files are runtime/ADR/tests/Cargo/CHANGELOG only. | Obtain unchanged-head gates and add realistic successful filesystem/recovery acceptance proving the same admitted object is moved reversibly and can be recovered without pathname substitution or unrelated-child loss. |
| PR #334 — ontology-backed parallel reclaim planning | `ab0f92c262fbf339f085c49a3597fad238bb7eb1` | stacked on exact #314; ontology-backed planning/platform-adapter owner | Draft, mechanically mergeable. A non-force descendant restack adopted exact #314 after its single-writer cleanup; this runtime lane also excludes the canonical product-gap baseline while preserving its causal platform Test delta. | Establish #264 then #314; prove real destructive/recovery acceptance per executable path and unchanged-head native/central gates before integration. |

The table is a dated routing aid, not a substitute for live GitHub queries. If any head/base moves, re-resolve the stack before mutation or merge classification.

## Current product contract

1. Scan and classification are read-only until a separately authorized reclaim boundary is reached. Logical size, allocated size, object identity, active use, materialization, provider state, ontology labels, and model advice remain distinct evidence.
2. User-file removal is reversible and journaled. Permanent user-file deletion is not a supported DiskSage action.
3. The ADR-0002 automatic regenerable-cache policy is a narrow reversible exception to second human approval: only fixed catalogued cache roots are eligible, and each direct child still requires fresh identity and complete active-use evidence before an OS-Trash move. Roots, unrelated children, DiskSage staging entries, active/ambiguous candidates, user files, provider data, and irreversible deletion remain outside that policy.
4. `--purge-proven-cache-trash` is inspection-only. `--execute --purge-proven-cache-trash` must fail before journal/filesystem mutation. ADR-0012 owns the requirements for any future same-object, full-descendant, freshness-bound, recoverable irreversible capability.
5. Cloud copy and local eviction are separate state transitions. Provider-local presence is not remote-sync proof; stale, incomplete, quota/auth/headroom, collision, active-use, placeholder, or identity evidence fails closed.
6. Models and optional ecosystem services may classify, explain, or recommend but cannot override deterministic safety evidence or mutate DiskSage domain truth.
7. Source/package metadata, local test results, open PRs, and development measurements are not immutable release evidence.
8. Settings persistence distinguishes an absent first-run file from an existing unreadable/unsafe file. A failed write must not be presented as persisted state or destroy the previous complete settings value; stronger post-rename power-loss durability requires separate platform evidence.

## Buyer-visible gaps

| Priority | Gap | Current evidence | Acceptance criterion |
| --- | --- | --- | --- |
| P0 | Safe reclaim must stay useful under real filesystem races and partial failures. | Active #314/#334 and cache-safety lanes cover temporary-artifact admission, symlink/hardlink/path replacement, process descendants, Podman/Colima/runtime storage, recovery receipts, and destructive-action boundaries. #339 separately records pathname-ancestor replacement and durable connection-document publication as unfinished filesystem work. | Real filesystem/process integration fixtures demonstrate no unreviewed deletion, bounded cancellation/timeouts, durable recovery evidence, and unchanged protected safety semantics across supported platforms. |
| P0 | The protected release line is blocked by dependency stacks and non-terminal hosted gates. | Current stacks include #264→#263→#315, #264→#338→#337, #264→#339→#212, #264→#202→#203, #264→#313, #264→#314→#334, #264→#199, plus #264→#195 and #264→#190. Queued/running exact-head workflows remain non-passing. | Dependency roots land first through normal protected merge with exact-head terminal gates; descendants remain non-force restacked and revalidated on unchanged heads. |
| P1 | Settings failures must not look like successful offline persistence. | #190 now has real-filesystem regression boundaries for missing-file first run, unreadable/unsafe paths, successful replacement, and rejected replacement preserving prior state. | Unchanged exact head passes native/central gates; unreadable existing state surfaces bounded actionable failure; failed replacement preserves previous bytes/value; unsafe paths fail closed; duplicate exposed settings writers are removed. |
| P1 | Cross-platform capability and failure semantics are broader than protected product evidence. | Windows/Linux/macOS adapters, provider-OAuth operational CLI work, and ontology-backed reclaim remain active PR evidence, not shipped truth. | Release notes and UI expose a verified platform capability matrix with real current-head integration evidence and bounded unsupported states. |
| P1 | Exact photo duplicates can be audited, but near-duplicate and cleanup decisions do not authorize mutation. | #313 provides bounded local PNG inspection, normalized raster identity, path/object identity checks, metadata/bit-depth evidence, and a Pareto keeper display while perceptual grouping and mutation remain unavailable. | Calibrate perceptual thresholds on a documented dataset; keep quality, lineage, and metadata evidence separate; require customer selection for unresolved ties; immediately revalidate identity/materialization/active use before mutation; move only through reversible Trash/quarantine with durable journal and tested undo/recovery. |
| P1 | TopFiles still lacks real browser interaction evidence. | #202 makes zero-result guidance reachable; #203 validates rendered structure and sequential focus, but SSR is not real keyboard/scroll proof. | Current-head browser E2E exercises normal/empty/loading/error states, Tab focus, keyboard scrolling, fragment continuity, visible focus, and long-path responsive overflow before resolving the interaction review. |
| P1 | Product documentation previously mixed historical cache-purge authority with the current fail-closed contract. | #315 reconciles PRD, ADR-0002/ADR-0012, runbook, README, architecture, and this baseline while preserving runtime ownership. | No current document reauthorizes permanent purge; status, preview schema, reversible policy, current PR routing, and protected-main maturity agree. |
| P2 | Repository-wide 100% docstring/test/edge-case coverage is not yet proven on one integrated protected head. | #337 converges coverage scope and fail-closed diagnostic evidence without lowering thresholds. | Exact integrated head produces non-vacuous 100% owned-production region/statement-equivalent, branch, function, line, public-doc, and edge-case evidence under repository policy. |

## Architecture and owner contracts

DiskSage owns its filesystem/reclaim facts. `.github`, `enterprise-architecture-core`, `context-graph-contracts`, `ConceptWeave`, `semantic-data-portal`, `contextual-orchestrator`, `noema`, `keyverse`, `EgressWeave`, `OriginWeave`, `pingora-gateway`, `quarantine-sandbox-runtime`, `appguardrail`, `wardnet`, `pg-llm-batch`, `EmbedRelay`, `fast-mlsirm`, `TEPP`, `RankWeave`, `ThreadWeave`, `inkspan`, `DiagramWeave`, and `mhtml-etl-gateway` remain canonical owners of their respective shared capabilities. DiskSage consumes only released/versioned contracts through explicit adapters/ACLs; source copies, cross-service application-table SQL, and mutable/unreleased production dependencies are defects.

For LLM work, `contextual-orchestrator` is the production routing/provider boundary. DiskSage does not own provider credential discovery or direct provider fallback. Deterministic safety, merge, security, coverage, and release gates remain independent of model judgment.

## Quality, security, accessibility, and performance gaps

- Destructive/recovery acceptance uses real filesystem/path/symlink/hardlink/mount/permission/race scenarios; synthetic data is unit-test support only.
- Material UI acceptance requires rendered normal/loading/empty/error/permission states plus real browser keyboard/focus/scroll evidence where interaction semantics matter. SSR structure is useful contract evidence but is not a substitute for browser interaction E2E.
- Rust remains the preferred hot-path, numerical, performance, and security runtime. Python requires an explicit bounded validation role where a practical Rust alternative is absent.
- Web/API buyer paths use async behavior and realistic E2E/load evidence when present. A p95 ≤20 ms claim is made only for a defined, measured path and environment; sampling, exclusion, or artificial cache warming must not manufacture a pass.
- Necessary personal data is protected by purpose-bound authorization, least privilege, minimization, encryption/retention where applicable, and access/export evidence rather than blanket masking that breaks the workflow.
- CSAP/SOC 2 language is evidence-readiness only unless an actual certification/attestation exists.

## Documentation and release acceptance

AGENTS.md, CLAUDE.md, PRD/TRD, ARCHITECTURE, Context Map/Ubiquitous Language, ADR index/details, UML/ERD or truthful logical data model, UX, SECURITY/THREAT_MODEL, TEST_STRATEGY, OPERABILITY/recovery, TRACEABILITY/doctoring, README, CHANGELOG, and this baseline must stay code-current. A GitHub Pages claim requires actual publish evidence.

A release is permitted only from one exact integrated protected head after applicable Test, security/SAST, exact coverage/docstrings, package/build, SBOM, provenance, reproducibility, review, rollback/recovery, accessibility, operability, and buyer acceptance are terminal-success. Then version metadata, CHANGELOG, tag, immutable package/release assets, provenance/SBOM, and rollback evidence must be created and verified together.

## Historical evidence

The detailed 2026-08-21/22 provider incident, local disk-pressure measurements, older PR topology, and earlier architecture notes were useful evidence but are not current routing authority. They are preserved in [the archived 2026-08-22 baseline](archive/product-technical-gap-baseline-2026-08-22.md) so prior incident evidence remains available while this canonical file stays current.
