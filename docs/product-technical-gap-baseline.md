# DiskSage product and technical gap baseline

The canonical product outcomes, supported capabilities, non-goals, and safety invariants are defined in [PRD.md](PRD.md). This document is the current implementation and ownership projection; runtime receipts, exact filesystem evidence, released contracts, protected refs, and live GitHub governance remain more authoritative than a dated snapshot. Historical incident detail is retained in `docs/archive/` rather than carried forward as current evidence.

**Snapshot:** 2026-09-11 (Asia/Seoul)

**Protected main:** `0e90f9cebadbd7f59606baaec4ca1d2f178c899a`

**Product boundary:** DiskSage owns Windows/Linux/macOS local-first disk-space inventory, filesystem classification/ontology, deletion safety, recovery, and platform adapters. Optional cloud, identity, ontology, LLM, security, and architecture capabilities consume released owner contracts through ports/ACLs; they do not become DiskSage mutation authority.

**Evidence rule:** queued, pending, skipped-required, failed, cancelled, stale, predecessor, synthetic-only, model-only, author-only, source-absent, or status-only evidence is non-passing. Every readiness, merge, release, or inheritance claim is bound to an exact current head. Mechanical mergeability is not release authorization.

## Current ownership projection

| Lane | Exact current state | Evidence and next boundary |
| --- | --- | --- |
| #264 — release/Test foundation | `e6de6fdb82dc49ca2a7bfffffeb99e68203206e6` on protected `main` ancestry | Release `34360428831`, Test `34360428838`, SAST `34360428882`, and Security `34360428851` are terminal SUCCESS. CodeQL `34360428776` is RED because the central current-head dispatch/terminal-verdict prerequisite has not settled; OpenCode/Noema/Strix and independent human governance also remain non-passing. No leaf synthetic status, provider override, or bypass is authorized. |
| #344 — reusable private filesystem publication | `938315bc6bbc709d248add203d046aec75fdc9d8` | Tests `34370477230` and `34370978864` are terminal SUCCESS. Unix/macOS create-new private publication is object-bound; existing-record replacement remains fail closed. Windows parent-handle/reparse/private-ACL/durability parity remains open. |
| #263 — cache-Trash irreversible boundary | `04e72faaade2dd17bdcc640106133554fd6fecc2` | Proposed ADR-0020 remains the canonical irreversible-deletion decision. `2ae01b89...` repaired the ADR-0012→ADR-0020 migration; current `04e72faa...` changes ADR-0020 only and records the platform capability boundary. Windows same-handle `FILE_ID_INFO` + `SetFileInformationByHandle` is a candidate implementation path, not enabled authority. Linux `openat2` and portable `openat`/`renameat`/`unlinkat` improve bounded traversal but are not treated as proof that the final recursive deletion targets the exact reviewed object. Linux/macOS remain fail closed. Draft #362 must preserve this canonical descendant while semantically adopting current #264. |
| #338 — shared Test owner | `442ea0da8dd6e4b5c01c49aa315f13549c2c2a29` | Exact Test `34524764055` is terminal SUCCESS. The owner now retains the authoritative original compound `npm test` transcript before any failure diagnostic rerun with `set -o pipefail`, a runner-temp transcript, and failure-only pinned artifact upload, while the final preservation step still reasserts the original failure. Downstream consumers may adopt this exact tested workflow delta through ordinary ancestry; success does not transfer to their product heads. |
| #339 — provider-OAuth domain owner | `1e3f434db9ae9f380f45848e91dfdffd75abd7a0` | Exact Test `34506068789` is terminal SUCCESS. The owner-local RED predecessor `961e736e7...` proved that whitespace, colon, non-ASCII, and interleaved/internal `=` bearer credentials could reach admission under the older validator. Current source now accepts the RFC 6750-style ASCII token alphabet and permits `=` only as trailing padding after a token character, failing malformed credentials before Authorization-header construction/provider dispatch. Existing OAuth document replacement remains unavailable until exact-source replacement, prior-value preservation, reparse resistance, and platform durability are proved. Draft Release is not release evidence. |
| #212 — shipped provider CLI/process/platform boundary | merged at source head `6589e0f2637361f831a3001d7430fabdc7bdc3de` into #339 | Test `34387297154` is terminal SUCCESS and the Windows lane actually executed source-present `provider_oauth_cli_process` with `cloud-cli`. The obsolete leaf Windows workflow remains removed; shared Test behavior stays #338-owned. |
| #371 — exact coverage/deletion-safety successor | `d388da66e5345bf368162420bffa58504e905c41` | Exact Test `34525026292` is terminal FAILURE. Windows, macOS, and real llama.cpp lanes are GREEN; Ubuntu passed Rust/backend/cloud/archive/npm-install stages before the frontend unit/l10n step failed. The exact nested frontend assertion is not yet stable evidence, so no product-source guess is authorized. Exact all-feature Rust production coverage is independently RED at 81.5280% regions (77,132/94,608), 60.8460% branches (6,387/10,497), 69.5556% functions (4,069/5,850), and 82.0036% lines (51,331/62,596). The diagnostic artifact `coverage-diagnostic-d388da66e5345bf368162420bffa58504e905c41` has digest `sha256:bfaaabce9406497ef4a59cccbcde7ec7575692a5b5a77e4ffbe50926933fca66`. No percentage transfers to a descendant. |
| #202 — scan/navigation owner | `74aa03cb75162c775455cb3ec67c143035b17594` | Tested #376 source `f48a57dbcaea41dc06899bd230b306fa1b84e1bf` normal-merged into #202 after Test `34431018121` terminal SUCCESS. The resulting owner disables Scan when no root is available without inventing fallback filesystem authority, and resulting-head Test `34435471818` is terminal SUCCESS. |
| #203/#373 — TopFiles accessibility/current-lineage integration | #203 `f3bfa0cd658627e72ebf3dc8fc077d44bde3f5f3`; #373 `7011c2c330d8ea20bf5a5520f43bf4338ee4c3ed` | Predecessor #373 `da079fe365c2189e9d12df556114939bf9f6eaeb` has exact Test `34439672546` terminal SUCCESS. An interim focus-outline hypothesis was reverted normally after review showed that visual outline geometry was not sufficient causal evidence for document scroll width. Current exact Test `34535969458` is nonterminal, so predecessor GREEN does not transfer. |
| #375 — real-browser evidence | `7f178131ab1e75ddf325a93133eda4e8ca75ee54` | Exact Test `34527975282` is terminal FAILURE, but the retained original transcript now localizes the buyer-visible RED. Vitest passed 68/68 files and 271/271 tests, workflow-concurrency contract passed, and Chrome launched successfully; the real-browser harness then failed `top-files-page-horizontal-overflow:375`. Failure artifact `npm-test-failure-34527975282-1` has digest `sha256:08c52be6a457e2fa19d327f9f8d7949cb65363707ebfa44d7d085f0e922c9ee5`. Root-only Chrome sandbox adaptation is therefore not the current blocker. The separate keyboard-scroll-region accessibility warning remains open. |
| #379 — narrow-window causal UI repair | `3d3b83ba428f144c0e0054eeb3441f96a280c5ec` based on #375 exact RED | Static current-lineage sizing proves Git-worktree path/reference/approval controls use `width: min(60rem, 90vw)` inside a page with browser body margin and padded `main`, while content-box border/padding can exceed the available 375px content width. The minimal repair adds `box-sizing: border-box` and `max-width: 100%` only to those controls; it does not hide overflow, truncate content, weaken the browser assertion, or change Git/deletion semantics. Exact Test `34536088881` is nonterminal; Windows, macOS, and real llama.cpp jobs are GREEN while the Ubuntu test job is still running. Keep Draft until exact resulting browser evidence settles. |
| #315 — canonical product/public docs | predecessor `dde29f15cc1cd010b4349457b201605c1d076f75`; this projection update | The predecessor projection was stale for #338/#371/#373/#375. This full-file update preserves the existing baseline while recording terminal #338 evidence, exact #371 coverage/failure, the localized real-Chrome #375 RED, the rejected TopFiles-outline hypothesis, and targeted #379 repair. Documentation does not confer runtime or release authority. |

The verified OAuth path is `#264 → #344 → #339`, with #212 already normal-merged into #339 after adopting the previously tested shared-Test owner and obtaining source-present Windows process evidence. Shared Test ownership remains `#264 → #338 → consumers`: current #338 exact `442ea0da...` is GREEN and can be adopted through ordinary ancestry, but that workflow evidence does not confer product-head success. Coverage work remains isolated in #371: current `d388da66...` is independently blocked by the 100% Rust denominator and has a separate Ubuntu frontend-step failure whose nested assertion is not yet stable enough for a causal source change. Current deletion safety retains #263's Proposed ADR-0020 fail-closed boundary. The platform analysis makes clear that Windows has a plausible same-handle implementation route while Linux/macOS do not gain permanent-delete authority merely from safer path-resolution APIs. UI work now has a concrete browser RED: #375 `7f178131...` launches Chrome and fails only at 375px page overflow after Vitest/workflow-contract GREEN. The rejected TopFiles-outline change was normally reverted; #379 isolates a mathematically bounded narrow-container defect in Git-worktree controls and must earn exact browser GREEN before any normal merge/restack.

## Product invariants

1. Scan and classification are read-only until a separately authorized reclaim boundary is reached. Logical size, allocated size, filesystem identity, active use, materialization, provider state, ontology labels, and model advice are distinct evidence.
2. User-file removal is reversible and journaled. Permanent general user-file deletion is not a supported DiskSage action.
3. Regenerable-cache policy never grants irreversible authority merely from names, signatures, byte counts, or approval phrases. `--purge-proven-cache-trash` remains inspection-only and shipped execution fails closed when identity-bound permanent-delete capability is unavailable.
4. A successful mutation must consume the exact reviewed object. Repeating pathname checks is not sufficient authority for replacement or irreversible deletion. Unsupported platform primitives fail before mutation rather than falling back to delete-and-create or recursive pathname deletion.
5. Cloud copy, provider sync proof, local presence, local eviction, and source removal are separate transitions. Stale/incomplete provider evidence, quota/auth/headroom ambiguity, collisions, active use, placeholders, or identity gaps fail closed.
6. Models and ontology may classify, explain, or recommend but cannot override deterministic filesystem/deletion evidence or mutate DiskSage truth. Model-backed repository workflows consume `orchestrator/free` through contextual-orchestrator and do not pin a paid/direct provider locally.
7. Settings, receipts, approvals, and recovery records distinguish first-run absence from an existing unreadable or unsafe document. Failed replacement must preserve the previous complete value.
8. Crash/power-loss and durability claims are platform-specific and must name the primitive actually exercised. macOS `fsync` evidence is not described as stronger `F_FULLFSYNC` evidence.
9. Shared workflow behavior has one canonical owner. Source-absent conditional success is admission evidence, not runtime proof.
10. Release readiness belongs only to the exact protected lineage after all applicable deterministic, security, model/review, provenance, reproducibility, rollback, and human-governance gates pass. Predecessor success never transfers automatically.

## Buyer-visible gaps

| Priority | Gap | Acceptance criterion |
| --- | --- | --- |
| P0 | Same-object mutation, Trash/recovery, and undo across Windows/Linux/macOS | Real-filesystem adversarial tests cover ancestor replacement, symlink/reparse/hardlink/FIFO substitution, mount and permission drift, staging replacement, partial mutation, crash/power-loss recovery, durable pending/terminal receipt, idempotent resume, and undo. Every success mutates the exact reviewed object; unsupported capability fails before mutation. Windows permanent cache deletion may advance only after same-handle `FILE_ID_INFO` identity plus handle-bound rename/disposition and recursive descendant evidence are proved. Linux/macOS remain unavailable until equivalent final-object binding or a separately accepted recoverable quarantine protocol exists. |
| P0 | Protected release and required-gate settlement | Canonical `.github` owners produce authenticated unchanged-head terminal CodeQL/model/review receipts for every applicable required workflow and independent human approval is satisfiable without self-approval. Then #264 can merge normally and release can proceed with immutable artifacts, SBOM, provenance, reproducibility evidence, and rollback. |
| P0 | Owned-production coverage at 100% | Exact-head frontend and Rust coverage reach 100% by executing meaningful production branches with realistic tests/fixtures. No threshold reduction, production exclusion, sample shrinking, or synthetic success. Current #371 exact `d388da66...` is terminal RED; its Rust production measurement is 81.5280% regions / 60.8460% branches / 69.5556% functions / 82.0036% lines and remains far below acceptance. The independent Ubuntu frontend-step failure must be localized before any product-source repair. |
| P0 | Cache-Trash owner integration after ADR collision repair | #263 preserves Proposed ADR-0020 and its fail-closed irreversible boundary while semantically adopting current #264 through ordinary ancestry. #263 exact `04e72faa...` includes the complete ADR migration plus platform capability analysis and must be preserved by #362; the resulting integration head must reacquire applicable tests before downstream release claims. |
| P1 | Windows native private-publication/replacement parity | Use native handle-bound namespace authority, reject reparse drift, enforce private ACL-equivalent policy, define data/namespace flush behavior, and prove replacement plus partial-failure recovery on Windows. |
| P1 | macOS durability precision | Tests and documentation distinguish exercised `fsync` behavior from stronger device-flush semantics such as `F_FULLFSYNC`; recovery claims match actual primitives. |
| P1 | Provider-OAuth existing-document update | Prove exact-source-object replacement, cleanup, reparse/symlink resistance, previous-value preservation on failure, and platform-specific durability before enabling update. Bearer credential grammar hardening is no longer an open gap on #339 exact `1e3f434...`: Test `34506068789` is terminal SUCCESS, and downstream consumers must adopt that exact owner delta through verified ancestry rather than copying the validator. |
| P1 | Receipt-parent first-use bootstrap | Create/adopt the exact private receipt parent under race-resistant authority, prove identity/mode/durability, and expose approval only when execution can reacquire that authority. |
| P1 | TopFiles browser accessibility/resilience | Current-lineage browser E2E covers normal/loading/empty/error/permission states, keyboard traversal and scrolling, visible focus, fragment continuity, CJK/long-path expansion, and responsive overflow without clipping or inert controls. #375 exact `7f178131...` has concrete real-Chrome RED `top-files-page-horizontal-overflow:375` after 68/68 Vitest files, 271/271 tests, workflow contract, and Chrome launch all pass. #379 targets the 375px Git-worktree control sizing defect without clipping or assertion weakening and must first earn exact Test/browser GREEN, then be normally integrated into #375 and reacquire resulting-head evidence. Full KO/EN/JA/ZH/VI/ES/DE/FR versioned resource-ledger/text-expansion/font-fallback acceptance and WebView2/WebKit parity remain required even after Chrome overflow is fixed. |
| P1 | Near-duplicate cleanup evidence | Calibrated thresholds and provenance/quality/lineage evidence precede any reversible quarantine/Trash action; fresh identity/materialization/active-use preflight, durable receipt, and tested undo are required. |

## Standards and owner traceability

- POSIX pathname operations such as `openat()`/`renameat()`/`unlinkat()` bind resolution to directory descriptors but do not by themselves prove that the final name-selected mutation consumed the exact previously reviewed directory object. Linux `openat2()` adds strong `RESOLVE_*` path-resolution constraints and remains valuable for inspection/traversal, but does not independently satisfy the final-object deletion invariant.
- Windows same-object permanent-delete research is tied to native handle-oriented semantics. Microsoft documents `FILE_ID_INFO` volume serial + 128-bit file ID as a same-file identity check between open handles, and `SetFileInformationByHandle`/`FILE_DISPOSITION_INFO` as handle-based rename/delete primitives requiring appropriate access. DiskSage does not enable this path until recursive descendant, reparse, ACL, partial-progress, crash/recovery, and real NTFS/ReFS evidence is present.
- macOS persistence claims distinguish ordinary `fsync` evidence from stronger `F_FULLFSYNC` semantics. Portable POSIX name-based final mutation is not promoted to object-bound permanent-delete authority merely because ancestor traversal is descriptor-relative.
- OAuth bearer credentials sent in the `Authorization` header follow RFC 6750 §2.1 syntax. #339 exact `1e3f434...` now enforces its admitted ASCII token alphabet/trailing-padding boundary before transport, with exact Test `34506068789` GREEN; downstream branches must consume the released/verified owner delta rather than maintaining a divergent local validator.
- GitHub Actions evidence is exact-head evidence only when the relevant source exists and the workflow actually executes it. Failed-job rerun/carry-forward records must not be treated as proof that every reported job physically executed on the same attempt. Compound package scripts require original-invocation evidence before a failed workflow step is attributed to a particular nested test or browser phase. Current #338 retains the original `npm test` transcript before failure-only diagnostics; #375's retained artifact then localizes its current original invocation to real Chrome 375px overflow instead of a rerun-only inference.
- Issue #170 owns broader deletion/recovery safety; issue #342 owns private-publication/exact-source replacement; issue #340 owns TopFiles browser acceptance. Central CodeQL/model workflow defects remain canonical `.github` owner work rather than DiskSage leaf substitutes.

No statement in this baseline grants irreversible deletion, cross-service SQL, mutable sibling dependency, source-copy authority, direct provider/model override, self-approval, or release-gate bypass. Live exact refs and released owner contracts supersede this snapshot whenever they advance.