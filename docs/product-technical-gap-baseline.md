# DiskSage product and technical gap baseline

**Snapshot:** 2026-08-30 (Asia/Seoul)

**Protected `main` observed:** `d2eab47dc5a33082938bc269c28faf2b82e326c4`

**Scope:** PRD, accepted ADRs, requested open PRs, and bounded local disk/cloud observations.
**Authority boundary:** this is inventory, not deletion, cloud-write, local-eviction, or merge authority. Mutation still requires fresh item identity, provider evidence, an exact plan fingerprint, required approval, and postconditions. GitHub conclusions apply only to the current PR head.

## Product contract

The integrated contract comes from `docs/product-requirements.md` and ADR-0001 through ADR-0011. PR #315 proposes `docs/PRD.md` as the canonical contract; it remains non-authoritative until that PR is integrated:

1. Inventory is read-only and metadata-first. Embedded metadata precedes an unambiguous filename token, then filesystem creation/modification metadata. A token such as `2026-04-28` or `251210` is secondary evidence, not ownership, upload, or eviction evidence.
2. Cloud offload follows `copy-verified -> pending-provider-sync -> provider-sync-confirmed -> eviction-ready -> source-evicted`. `local-current` with `is_uploaded=false`, stale/incomplete evidence, active transfer, mismatch, conflict, or unknown state cannot issue an eviction permit.
3. Copy, adoption, and local-copy eviction are bounded and identity-bound. Provider placeholders are not read merely to classify them. Success and failure receipts remain distinct; private path-bearing recovery records are not public exports; mutation is revalidated immediately before execution.
4. Regenerable caches and user data are separate reclaim domains. A cache action requires its implemented identity, active-use, journal, and recovery contract; it is not cloud offload.
5. Deterministic Rust gates own safety. Models may explain or rank already-admissible actions but do not grant mutation authority. OAuth and external services are optional for the standalone desktop product.

## Exact open-PR inventory

`blocked`, `unstable`, `dirty`, and `unknown` are captured GitHub merge states, not source-quality conclusions. A stacked PR is not integrated into `main` merely because its own checks pass.

| PR | Exact head | Base | State | Responsibility still open |
| --- | --- | --- | --- | --- |
| #247 | `ff70f8159b82f05593c2ae7611ab3a5229ae886f` | `main` | draft, blocked | Explain pending iCloud indexing without granting transfer authority |
| #263 | `93e5d5685074c146a5b935c6de401b34891616e0` | `main` | ready, blocked | Keep permanent cache-Trash deletion unavailable and exclude provider-managed cache roots |
| #266 | `fa463566759ba8943d435b05bbed6aff6b896cc0` | `main` | ready, blocked | Prevent placeholder materialization and withhold rejected provider roots from cleanup flows |
| #267 | `69adbf00e50d63cb481556a12b9fb6e0148aa8f8` | `main` | ready, blocked | Runtime-agnostic container orphan reclamation with durable, outcome-specific capacity receipts; hosted checks include failures and no exact-head approval exists |
| #282 | `7be69db1a46c3cd6cc8cd94b5c103f48336e5c7a` | `feat/container-orphan-reclaim-runtime-agnostic-v1` | ready, unstable | Repair Podman storage before orphan reclaim |
| #285 | `4d1fd87b41e79e59207f7086c135515453551056` | `feat/podman-storage-repair-v1` | ready, unstable | Gate native uv-cache pruning |
| #287 | `25dbb2095c9a1a30da5dc8fc2ef3e37bb2cdd987` | `feat/podman-storage-repair-v1` | ready, unstable | Preserve stopped containers with storage lineage |
| #295 | `b7fb5fde8427d2e38fa29dc868fd4d371dee3519` | `feat/native-uv-cache-reclaim-v1` | ready, unstable | Reclaim observed macOS generated caches through reviewed identity, inactivity, provider-exclusion, exact approval, and outcome receipt gates; hosted checks include a failure and no exact-head approval exists |
| #298 | `ebcd89508443d0081447b61d60b94a495a6b9ce7` | `main` | ready, blocked | Bound test-runner disk use |
| #303 | `16406f3182fdfdc5f1922ed29fd6b3685c9de2bf` | `main` | ready, blocked | Evidence-bound provider-cache reclaim |
| #304 | `6f1ac405b3f689cd89b02397000fa0d74971bb38` | `main` | ready, blocked | Diagnose OneDrive temporary-storage pressure and offer an exactly reviewed quit, reopen, and rescan path without reset, provider-storage deletion, OAuth, or validation-time live mutation; hosted checks are queued or running, three unresolved threads are information-only, and no exact-head approval exists |
| #305 | `0ae083005232f9e2f8a76ccc9423546abffc54c5` | `main` | ready, blocked | Reclaim explicitly identified PostgreSQL test clusters |
| #306 | `bb22a5b3683227477e4606d2c1a45c129efbe333` | `main` | ready, blocked | Reclaim Python tool caches while excluding provider-managed roots |
| #308 | `67306e9c262d76d721edde8edf2c38a96e125956` | `main` | ready, blocked | Bind release verification to platform artifact namespaces |
| #309 | `6b8ee47bea68d10da090ab35ff7219b0d7af93e3` | `main` | ready, blocked | Reclaim selected development build roots |
| #310 | `9f8db866f617ad85a35640151276af524ccc51dc` | `main` | ready, blocked | Add read-only, fail-closed Colima disk reclaim planning |
| #311 | `94246aa27e7aef7fe1c9a7e03bf374d884067326` | `feat/native-uv-cache-reclaim-v1` | ready, unstable | Plan and Trash standalone clones only with closed/merged exact authority after fresh repository, provider, identity, and active-use validation; the current base is merged normally, focused validation passes, review threads are resolved, one hosted check fails, and no exact-head approval exists |
| #312 | `38c83992b8d7a2ab69a6db44963ecc526b1ebb86` | `fix/release-artifact-namespace-v2` | ready, clean | Execute an exactly approved Parallels compaction only after native configured-disk identity, an exact zero snapshot count, stopped/inactive state, allocation, and provider-root checks. Empty snapshot output yields a keep-local rescan plan; all hosted checks pass and no exact-head approval exists |
| #313 | `2281eb634d3e56de6b580465cc70dfccfa341826` | `main` | ready, blocked | Group exact decoded PNG pixels and expose only a uniquely Pareto-dominant keeper; calibrated near-duplicate/IQA evidence and execution remain unavailable |
| #314 | `2843bed84dd5ac1910539438af75f2cfec5e1c8f` | `fix/release-artifact-namespace-v2` | ready, clean | Reclaim admitted generated build roots from native temporary storage with evidence-bound planning and reversible execution |
| #315 | `ebf0d8caaca0db1c59df676e96f7e00378400855` | `main` | ready, blocked | Establish `docs/PRD.md` as the canonical product contract and link live implementation traceability back to this baseline |
| #316 | `300b1556504912871febab231003147fd9777da3` | `feat/podman-storage-repair-v1` | ready, unstable | Review exact photo duplicates, choose an unambiguous keeper, and use reversible quarantine; Linux approval, fail-closed Windows compilation, native active-use/path handling, chronology, multi-encoding comparison, repeat-scan scope, and direct-selection scope repairs are present; review threads are resolved, hosted checks are running, and no exact-head approval exists |
| #317 | `47e1715a14ddf951c461744039d0f15f6c56817c` | `feat/photo-duplicate-quarantine-integration-v1` | ready, clean | Review the approximately 20 GiB managed Apple Photos library through PhotoKit without traversing its package, require an explicit keeper, and preserve fsynced prepared/completed deletion outcomes; the base is merged normally, review threads are resolved, hosted checks pass, and no exact-head approval exists |
| #318 | `7f3ffdbe1c563c8db2df4f2b6f86ecbdd8209cc2` | `main` | ready, blocked | Return a bounded keep-local receipt for an inconclusive provider probe, separate shared-volume movement from completed action-attributable receipts, and keep customer guidance action-first; review threads are resolved, hosted checks include failures, and an independent exact-head approval is still required |
| #319 | `16ea2dbddfd988245070640ae49a3f29aef6549c` | `main` | ready, blocked | Show which apps must be closed before rescanning deleted-but-open regular files, without exposing paths, terminating processes, mutating files, or presenting logical size as guaranteed physical recovery; review threads are resolved, product tests and builds pass while review/dependency gates fail, and no exact-head approval exists |
| #320 | `427d78aa8fb9d0e5fb35e32ddd065f38966ff1da` | `feat/macos-generated-cache-reclaim-v1` | ready, unstable | Fingerprint cache-internal symlinks without resolving or traversing their targets while keeping root symlinks and special files blocked; a hosted check fails and no exact-head approval exists |
| #321 | `f66c3fa828980fe9ec11385cc27bb0b5c6aa8575` | `feat/apple-photos-duplicate-library-v1` | ready, clean | Checkpoint a bounded PhotoKit inventory with ordered identity, bounded page deltas, one-time finalization, and inspect-only cancellation/resume while keeping network access and Photos mutation disabled; the base is merged normally, hosted checks pass, and no exact-head approval exists |
| #322 | `740eaa9aa8fbdb054a894d5cdaa0ad134540f98c` | `feat/generated-cache-symlink-manifest-v1` | ready, unstable | Exclude DiskSage and its bounded helper probes from active-use evidence so self-observation does not permanently block reviewed cache actions; a hosted check fails and no exact-head approval exists |
| #323 | `2534476ac3d62aad55fa88b54509769897d62f35` | `fix/active-use-self-pid-v1` | ready, unstable | Produce a bounded, read-only whole-home allocation map without following symlinks or crossing filesystem devices; partial scans carry an exact stop reason, focused validation passes, hosted checks are pending, and no exact-head approval exists |
| #324 | `351a7d5a4819e251033f5c3c014604e9845e4e0c` | `feat/evidence-accounting-presentation-v1` | ready, unstable | Checkpoint third-party global-sync observations with path-free SHA-256 lineage and the documented five-minute automatic backoff; a clear global queue stops polling but never grants item eviction, review threads are resolved, hosted checks are pending, and no exact-head approval exists |
| #325 | `aef79d23ec9f25474561bb06ecff1923a23f422a` | `fix/active-use-self-pid-v1` | ready, unknown | Reclaim only inactive DiskSage-owned Cargo targets directly under the macOS per-user temporary root through exact plans, fresh approval, atomic staging, and terminal receipts; one hosted check fails and no exact-head approval exists |

PR #258 itself was observed before this edit at `16556c738d34e75d90fc31902db6dd45825599a6`, open, non-draft, and `BLOCKED`, with no exact-head approval. Its live head after this document changes is authoritative. The central workflow repair [ContextualWisdomLab/.github #1415](https://github.com/ContextualWisdomLab/.github/pull/1415) was observed at exact head `f0c768fe9e48444b8bac14d3fa2957db4e7d5585`, open and `BLOCKED`, with no exact-head approval and hosted OpenCode, Noema, and Strix failures plus a cancelled queue scan; it is not passing evidence for any DiskSage head.

## Bounded operational observations

These diagnose pressure; none authorizes mutation. Private evidence and receipt locations stay local with restrictive modes. This public baseline records no path, account identifier, or user content name.

| Observation | Bounded result | Consequence |
| --- | --- | --- |
| APFS global sample, 2026-08-30 14:23 KST | Data volume `808,529,660 KiB` used and `96,609,352 KiB` available (`98.93 GB`, `92.13 GiB`) | This shared-volume sample fluctuates with concurrent activity and is not reclaim attribution. The 300 GB product goal remains unmet; keep it separate from action receipts and re-sample immediately before and after each approved action. |
| Evidence-gated registered-worktree cleanup, 2026-08-30 | NUL-safe inventory decreased from 45 to 40 after five exact closed/merged-head worktrees passed clean tracked/untracked, inactive, non-default, and no-unique-commit gates; removed worktrees accounted for `45,808 KiB` allocated (`13,820,218 B` tracked logical), with branches preserved | These allocated bytes are action-attributable. The much larger concurrent APFS availability change is not. Productize the same fresh remote-head and filesystem-identity gates before offering this action. |
| Evidence-gated registered-worktree cleanup, later pass, 2026-08-30 | Eleven additional exact closed/merged-head worktrees passed the same gates; registered count decreased from 41 to 30 and the removed roots accounted for `42,428 KiB` allocated, with branches preserved | The pass's global APFS availability changed from `108,319,724 KiB` to `108,446,760 KiB` (`+127,036 KiB`) while other processes were active. Keep the `42,428 KiB` attributable allocation and the global volume delta as separate measurements; do not add them together. |
| Generated and temporary validation cleanup, 2026-08-30 | Bounded validation cleanups reported approximately `2.41 GiB`, `3.87 GiB`, `5.9 GiB`, and `4.01 GiB` of disposable generated roots across separate worktrees; a separate temporary-root pass coincided with an approximately `1.86 GiB` APFS availability increase | Command- or root-accounted deleted bytes are logical/allocated action evidence. The observed APFS change is a shared-volume sample affected by concurrent builds, snapshots, sparse allocation, and provider activity; it is neither additive nor proof of equivalent physical recovery. |
| Generated-root reclaim, round 14, 2026-08-30 | The admitted generated roots accounted for `7,366,036 KiB`; the immediate shared-volume availability increase was `4,741,996 KiB`, while the settled observed net increase was `3,296,160 KiB` | Report the admitted allocation, immediate global delta, and settled global net separately. Their differences can reflect concurrent writes, snapshots, sparse allocation, and delayed filesystem accounting; do not add these values together. |
| Completed user/cache reclaim receipts, 2026-08-30 | Fresh DiskSage outcome receipts account for `1,118,683,136 B` of completed user/cache actions | This byte total belongs to completed action receipts only. Do not add the later shared-volume samples or validation cleanup to it, and do not treat it as progress toward the 300 GB target without the separate APFS postcheck. |
| Homebrew cleanup postcheck, 2026-08-30 | The approved Homebrew cleanup coincided with a `7,044 KiB` APFS availability increase | Keep this bounded before/after shared-volume delta separate from the completed-receipt byte total. Concurrent APFS activity means it is an observed postcheck, not an additive command-attributable receipt. |
| Focused validation cleanup for #320 and #321, 2026-08-30 | Cleaning #320's generated audit target coincided with a `4,144,652 KiB` APFS availability increase; cleaning #321's build output coincided with a separate `2,388,184 KiB` increase | These are generated validation artifacts, not user-data recovery and not completed product-action receipts. The samples overlap concurrent shared-volume activity and prior observations, so do not add them to each other or to any reclaim total. |
| DiskSage temporary Cargo-target receipts, PR #325 exact head `aef79d23ec9f25474561bb06ecff1923a23f422a` | Two completed receipts record `2,845,298,688 B` and `4,989,251,584 B`, totaling `7,834,550,272 B`; both state `removed=true` and `provider_data_mutated=false`. The execution window's shared APFS availability increased `7,161,544 KiB` | Count the receipt total only as action-attributable temporary build allocation. Keep the smaller concurrent APFS delta separate; neither value is cloud eviction or user-data recovery, and they must not be added together. |
| Parallels inventory and compaction boundary, PR #312 exact head `38c83992b8d7a2ab69a6db44963ecc526b1ebb86` | Read-only native evidence measured one stopped VM bundle at `103,254,152 KiB` with one configured and one on-disk `.hdd` at `103,226,404 KiB`; no unused disk was proven. The snapshot command returned no JSON, so the plan remains inconclusive and non-executable. The implementation binds native configured-disk identity/count, requires an exact zero snapshot count, and preserves blank output as a keep-local rescan receipt; no live VM was mutated | Keep the VM and disk unchanged. Open Parallels and rescan snapshot state; compact only after an exact plan proves zero snapshots and receives fresh approval, then count only receipt-backed physical recovery. |
| Bounded whole-home allocation map, PR #323 exact head `2534476ac3d62aad55fa88b54509769897d62f35` | Read-only probes measured Mobile Documents `41,275,700 KiB`, Application Support `44,091,144 KiB`, Group Containers `16,582,836 KiB`, and `/private/var` `25,965,024 KiB`. The broader scan was cancelled after exceeding its observation budget; no deletion authority was inferred | Review the largest complete allocation domains first. Resume with explicit entry and duration budgets; preserve every partial or provider-managed result until its owning adapter proves a safe action. |
| Provider readiness checkpoint, PR #324 exact head `351a7d5a4819e251033f5c3c014604e9845e4e0c` | Three existing provider/eviction records contained zero provider-sync-confirmed or eviction-ready items and zero confirmed bytes. The checkpoint records blocked observations without paths, waits five minutes before automatic re-observation, and preserves provider/time lineage; a globally clear queue still requires a fresh item-specific check | Keep every local source. Let the provider finish, then rescan; proceed only when the exact item proves current upload, identity, content, conflict-free state, inactivity, and eviction capability. |
| OneDrive temporary storage, 2026-08-30 | The current read-only observation still reports `18,095,132,672 B` allocated, now represented by one file; provider state is not deletion or reset authority | Quit OneDrive from its supported menu, reopen it, and rescan only when the diagnostic offers that action. Do not delete temporary storage, reset the provider, or terminate its process. |
| Current iCloud native revalidation, 2026-08-30 14:23 KST | The bounded native observation still reported `client:needs-sync` with `needs-sync-up` and `needs-sync-down`; its reported last sync was 14:14 KST. No complete item-specific eviction cohort is available | Keep the local source. Let iCloud finish, then refresh the complete native inventory; `local-current` with `is_uploaded=false`, incomplete, or timed-out evidence cannot issue an eviction permit. |
| Managed Apple Photos library, 2026-08-30 | The approximately 20 GiB library is now covered by the separate PhotoKit review path in #317; direct package traversal remains prohibited and no live photo deletion or physical recovery was measured | Connect Photos only when ready to review exact groups. Choose one keeper per group and count recovery only after Photos confirms the change and DiskSage records the final receipt. |
| Retained OneDrive redacted bounded audit | top-128 cohort allocated `5,272,006,656 B`; not whole-root authority | Historical inventory is not a current plan. Require fresh native state, fingerprint, bounded approval, and postcondition. Temporary storage is not this authority. |
| Retained Google Drive redacted bounded audit | four root types; meaningful candidates `0 B`; incomplete lower-bound scan 59 entries / `319,488 B` | Keep generic eviction fail-closed. This evidence does not justify provider-generic executor expansion. |

## Customer-observable gaps

| Priority | Observable gap | Current evidence | Acceptance proof |
| --- | --- | --- | --- |
| P0 | DiskSage cannot yet demonstrate the requested 300 GB reclaim outcome. | APFS and the open cache/container/worktree/provider queue show independent domains, but no approved aggregate plan exists. | Fresh path-free inventories partition bytes by authority; bounded plans avoid double counting; APFS postchecks report actual reclaimed bytes. |
| P0 | Provider pressure can be visible while safe local-copy eviction is zero or stale. | The latest bounded iCloud state remains `needs-sync`; OneDrive reports about 17 GiB of temporary-storage pressure but no delete/reset authority; Google Drive has 0 meaningful candidates. The current receipt audit found zero confirmed or eviction-ready items. #318 adds an inconclusive keep-local receipt and #324 adds checkpointed re-observation, but neither is integrated into `main`. | Show the evidence time and next supported action: wait and rescan iCloud, or quit/reopen/rescan OneDrive when offered. Keep local sources and provider storage unchanged. |
| P0 | Scanning an ancestor of a provider root may materialize placeholders. | PR #266 remains open. | A fixture proves managed descendants are pruned or queried through a non-materializing capability before regular-file reads. |
| P0 | Container and generated-cache reclaim is fragmented across stacks. | #267/#282/#285/#287/#295 and #303/#305/#306 are not integrated into `main`. | Each domain reports identity, active-use/lineage blockers, dry-run bytes, bounded recovery semantics, journal, and APFS postcheck; bases merge in order. |
| P0 | Virtual-machine disk reclaim is not integrated. | Podman work is split across #267/#282/#287; #310 provides read-only Colima planning, while #312 now provides evidence-bound Parallels planning and approved native compaction with immutable outcomes. Neither stack is integrated into protected `main`, and no live Parallels recovery has been measured. | Keep the selected VM stopped and unchanged, review the exact plan, approve only that disk, and verify the before/after physical recovery receipt after required checks and the stack integrate. |
| P0 | Photo quality-aware duplicate cleanup is not integrated. | #313 provides exact decoded-pixel PNG evidence, #316 adds customer review and reversible quarantine, #317 adds a native PhotoKit path for the managed Apple Photos library, and #321 adds bounded checkpointed inventory. All remain outside protected `main`; perceptual near-duplicate grouping and calibrated no-reference IQA remain unavailable. | Complete or resume the current read-only inventory, review exact groups, keep every selected item inside the displayed scope, choose one keeper per group, and verify the final restore/change receipt before counting recovery. |
| P0 | Deleted files can continue occupying capacity while an app keeps them open. | #319 adds a bounded, path-free read-only audit and tells the person which apps to close, but it remains outside protected `main`; logical file size is intentionally not presented as guaranteed physical recovery. | Close the listed apps normally, rescan, and count recovery only from the separate shared-volume postcheck after the deleted-open identities disappear. |
| P1 | Merged/closed worktree and standalone-clone cleanup are not shipped end to end. | Two bounded operational passes safely removed 16 registered worktrees (`88,236 KiB` attributable allocation), but concurrent additions mean the inventories were 45→40 and 41→30 rather than one continuous count. The capability is not on protected `main`; standalone clone lifecycle #311 is stacked and unstable. | Refresh the remote PR state, default branch, commit reachability, tracked and untracked changes, active use, and filesystem identity immediately before reversible removal; preserve every uncertain repository. |
| P1 | `/tmp` and tool-cache reclaim is only partially integrated. | #314 implements evidence-bound generated-root reclaim, #320 keeps internal symlinks non-traversing, #322 excludes DiskSage's own probes from active-use evidence, and #325 has receipt-backed support for DiskSage-owned macOS Cargo targets. Clone/worktree, nonce-scoped, and general temporary-file adapters remain unavailable; these PRs are not integrated into protected `main`. | Review only producer-owned regenerable roots, then verify current identity, provider exclusion, inactivity, exact approval, terminal receipt, and the separate APFS postcheck. Unknown temporary content remains review-only. |
| P1 | Release evidence is not uniformly namespace-bound. | #308 remains blocked. | Verify every platform artifact, checksum, attestation, and help contract against its exact namespace and source head. |
| P1 | Repository-wide 100% coverage/docstring/edge claims are not evidenced. | No complete measured report is present. | Publish per-language executable measurements and claim only measured scopes. |

## Technical and operational gaps

| Priority | Gap | Smallest next proof |
| --- | --- | --- |
| P0 | No fresh aggregate reclaim plan is bound to current disk/provider observations. | Produce complete independent inventories; deduplicate extents/candidates; fingerprint bounded plans; revalidate at mutation. |
| P0 | Provider temporary storage has no safe generic deletion authority. | Use provider-supported ownership/status evidence. Never infer staleness from age; add an action only with a proven provider contract and postcondition. |
| P0 | Stacked branches can contradict one another until bases integrate. | Process base-first: current-head review, verified fixes, terminal required checks, qualifying review, normal merge, then refresh the next head. |
| P1 | APFS observations are not yet integrated with completed action-receipt totals. | #318 separates global available-space movement from unique completed action receipts, but it remains open. Integrate it, then bind path-free before/after snapshots and the action receipt set to the same reviewed run without reconciling the two totals. |
| P1 | Runtime UI evidence is weaker than fixture coverage for provider and PhotoKit edge states. | Capture bounded signed-artifact E2E receipts without hydrating placeholders or exposing paths; #321 resolves the observed pagination, equal-count identity drift, cancellation, and resource-cutoff findings but remains pending hosted checks and integration. |
| P2 | Optional ontology/agent/catalog integrations can drift beyond measured need. | Keep adapters over path-free receipts and relations; add a dependency only with a consumer, failure contract, and standalone-offline test. |

## PRD, ADR, and research consistency

- [`docs/product-requirements.md`](product-requirements.md) remains the integrated PRD. [PR #315](https://github.com/ContextualWisdomLab/disksage/pull/315) proposes canonical `docs/PRD.md`; until integration, its exact head is inventory evidence rather than mutation authority.
- [ADR-0001](architecture/adr/0001-cloud-offload-goal-state.md) defines exact-copy evidence and sync-gated eviction; [ADR-0002](architecture/adr/0002-cache-cleanup-is-per-item-evidence-bound.md) separates cache reclaim; [ADR-0004](architecture/adr/0004-bounded-maintenance-command-execution.md) bounds maintenance commands; [ADR-0006](architecture/adr/0006-redacted-icloud-health-evidence.md) and [ADR-0007](architecture/adr/0007-pre-copy-evidence-cohort.md) govern redacted, temporally coherent iCloud evidence.
- [ADR-0008](architecture/adr/0008-hourly-loop-foreign-dependencies-read-only.md) keeps the hourly agent loop read-only across foreign dependencies. [ADR-0009](architecture/adr/0009-path-free-lineage-relation-graph.md) defines path-free lineage exports. [ADR-0011](architecture/adr/0011-cloud-transfer-failure-and-materialization.md) requires durable failed-copy evidence and placeholder-safe adoption.
- Accepted ADR meaning is append-only. Contradictory implementation evidence requires a superseding ADR, not silent rewriting.
- Standards and APA 7th research citations remain in owning ADR/specification documents. PR #313's ADR separates exact decoded-pixel identity and Pareto evidence from unavailable calibrated perceptual/IQA artifacts; this baseline introduces no threshold, composite weight, provider guarantee, or speculative execution authority.

## Loop completion rule

For every row: refresh exact remote head/base; inspect all current threads and required checks; fix verified source defects at the owning boundary; run focused tests; push normally; re-query the new exact head. Merge only when rules, terminal required checks, and qualifying review are satisfied. Waiting, cancelled, stale, or provider-failed checks are evidence to classify, not bypass permission. After merge, refresh dependent stacks and this baseline.
