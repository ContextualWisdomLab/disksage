# DiskSage product and technical gap baseline

The canonical product outcomes, supported capabilities, non-goals, and safety invariants are defined in [PRD.md](PRD.md). This document is the current implementation and ownership projection; runtime receipts, exact filesystem evidence, released contracts, protected refs, and live GitHub governance remain more authoritative than a dated snapshot. Historical incident detail is retained in `docs/archive/` rather than carried forward as current evidence.

**Snapshot:** 2026-09-11 (Asia/Seoul)

**Protected main:** `0e90f9cebadbd7f59606baaec4ca1d2f178c899a`

**Product boundary:** DiskSage owns Windows/Linux/macOS local-first disk-space inventory, filesystem classification/ontology, deletion safety, recovery, and platform adapters. Optional cloud, identity, ontology, LLM, security, and architecture capabilities consume released owner contracts through ports/ACLs; they do not become DiskSage mutation authority.

**Evidence rule:** queued, pending, skipped-required, failed, cancelled, stale, predecessor, synthetic-only, model-only, author-only, source-absent, or status-only evidence is non-passing. Every readiness, merge, release, or inheritance claim is bound to an exact current head. Mechanical mergeability is not release authorization.

## Current ownership projection

| Lane | Exact current state | Evidence and next boundary |
| --- | --- | --- |
| #264 — release/Test foundation | `e6de6fdb82dc49ca2a7bfffffeb99e68203206e6` on protected `main` ancestry | Release `34360428831`, Test `34360428838`, SAST `34360428882`, and Security `34360428851` are terminal SUCCESS. CodeQL `34360428776` is RED because the central current-head dispatch/terminal-verdict prerequisite has not settled; OpenCode/Noema/Strix and independent human governance also remain non-passing. No leaf synthetic status, provider override, or bypass is authorized. |
| #344 — reusable private filesystem publication | `938315bc6bbc709d248add203d046aec75fdc9d8` | Tests `34370477230` and `34370978864` are terminal SUCCESS. Unix/macOS create-new private publication is object-bound; existing-record replacement remains fail closed. Windows parent-handle/reparse/private-ACL/durability parity remains open. |
| #263 — cache-Trash irreversible boundary | `04e72faaade2dd17bdcc640106133554fd6fecc2` | Proposed ADR-0020 remains the canonical irreversible-deletion decision. `2ae01b89...` repaired the ADR-0012→ADR-0020 migration; current `04e72faa...` records the platform capability boundary. Windows same-handle `FILE_ID_INFO` + `SetFileInformationByHandle` is a candidate implementation path, not enabled authority. Linux `openat2` and portable `openat`/`renameat`/`unlinkat` improve bounded traversal but are not treated as proof that the final recursive deletion targets the exact reviewed object. Linux/macOS remain fail closed. Draft #362 must preserve this canonical descendant while semantically adopting current #264. |
| #338 — shared Test owner | current `ca1324d90a8e5052935723b73e8151220148d0b3` | Contract-first `a43f42475ce225cb050e0ebd1f18726ad37d78bf` exposed the missing source-present macOS `provider_global_sync_success_pipe_contract` admission and requires the named step, exact locked cargo command, source-absent honesty, and non-zero status propagation. Current `ca1324d...` adds only that canonical macOS lane and its admission contract. Exact Test `34577850939` is terminal SUCCESS. The verified owner delta has been ordinary/non-force adopted by #386 and inherited by #387/#389. |
| #339 — provider-OAuth domain owner | `1e3f434db9ae9f380f45848e91dfdffd75abd7a0` | Exact Test `34506068789` is terminal SUCCESS. The owner-local RED predecessor `961e736e7...` proved that whitespace, colon, non-ASCII, and interleaved/internal `=` bearer credentials could reach admission under the older validator. Current source accepts the RFC 6750-style ASCII token alphabet and permits `=` only as trailing padding after a token character, failing malformed credentials before Authorization-header construction/provider dispatch. Existing OAuth document replacement remains unavailable until exact-source replacement, prior-value preservation, reparse resistance, and platform durability are proved. Draft Release is not release evidence. |
| #212 — shipped provider CLI/process/platform boundary | merged at source head `6589e0f2637361f831a3001d7430fabdc7bdc3de` into #339 | Test `34387297154` is terminal SUCCESS and the Windows lane actually executed source-present `provider_oauth_cli_process` with `cloud-cli`. The obsolete leaf Windows workflow remains removed; shared Test behavior stays #338-owned. |
| #371 — exact coverage/deletion-safety successor | current `be858f728446c659b95b97a0a91f12bcb6b6d125` | Exact Test `34551954662` is terminal FAILURE solely at the substantive 100% Rust production-coverage evidence gate after ordinary Rust/backend/cloud/archive, frontend, Windows, macOS and real llama.cpp paths pass. Current exact coverage remains 81.527989% regions (77,132/94,608), 60.845956% branches (6,387/10,497), 69.555556% functions (4,069/5,850), and 82.003642% lines (51,331/62,596). The largest uncovered production area remains `commands.rs`. No denominator, threshold, source exclusion, or predecessor transfer is authorized. |
| #383/#384/#385/#386/#387/#389 — Unix subprocess/process-group lifecycle | #383 `445ff35fd0ef1744d5d535692c6983566a01e97f`; #386 `a1d1ccebc70f16f2d06aafd1be86c2c8a6b626a0`; #387 `dd942d4187094f0621dc0ec16a982aea59806946`; #389 `aa3ada36c0f393919162af1983fda282ea738fa4`; issues #384/#385 open | #383's process-local mutex is test-harness stabilization only. Exact Tests #386 `34582209272`, #387 `34582250033`, and #389 `34582308667` are all terminal SUCCESS after ordinary/non-force adoption/restack of terminal-GREEN #338. #386 proves the reusable no-reap identity primitive and source-present macOS runtime/provider admission, #387 proves the container adopter, and #389 proves the File Provider adopter with the safe descendant-held-stdout fixture. Protected release/governance and broader caller migration remain separate blockers; #384/#385 therefore stay open. |
| #202 — scan/navigation owner | `74aa03cb75162c775455cb3ec67c143035b17594` | Tested #376 source `f48a57dbcaea41dc06899bd230b306fa1b84e1bf` normal-merged into #202 after Test `34431018121` terminal SUCCESS. The resulting owner disables Scan when no root is available without inventing fallback filesystem authority, and resulting-head Test `34435471818` is terminal SUCCESS. |
| #203/#373 — TopFiles accessibility/current-lineage integration | #203 `f3bfa0cd658627e72ebf3dc8fc077d44bde3f5f3`; #373 `7011c2c330d8ea20bf5a5520f43bf4338ee4c3ed` | Predecessor #373 `da079fe365c2189e9d12df556114939bf9f6eaeb` has exact Test `34439672546` terminal SUCCESS. An interim focus-outline hypothesis was reverted normally after review showed that visual outline geometry was not sufficient causal evidence for document scroll width. Current downstream browser evidence is owned by #375/#379/#380; predecessor GREEN does not transfer. |
| #375 — real-browser evidence | `7f178131ab1e75ddf325a93133eda4e8ca75ee54` | Exact Test `34527975282` is terminal FAILURE and localizes the original buyer-visible RED. Vitest passed 68/68 files and 271/271 tests, workflow-concurrency contract passed, Chrome launched, and the real-browser harness reported `top-files-page-horizontal-overflow:375`. Failure artifact `npm-test-failure-34527975282-1` has digest `sha256:08c52be6a457e2fa19d327f9f8d7949cb65363707ebfa44d7d085f0e922c9ee5`. That RED is retained as causal history, not current UI status. |
| #379/#388 — responsive UI + browser teardown lifecycle | #379 `5b533ca665d12c4efbe9f633dc9a4055b11ad129`; issue #388 closed completed | Component-local long-token/intrinsic-sizing repairs removed the real-Chrome 375px document overflow without global clipping, viewport reduction, or tolerance widening. Browser teardown then exposed a separate profile-removal race. Contract-first `0de3d848...` required browser-level `/json/version`, CDP `Browser.close`, launched-process exit proof, and no retry widening. Current #379 implements that lifecycle and exact Test `34572975401` is terminal SUCCESS on the unchanged head after rerunning only the failed Ubuntu job; Windows, macOS and real llama.cpp are also GREEN. Issue #388 is closed as the lifecycle defect only. |
| #380 — TopFiles keyboard-scroll-region a11y repair | `6b5b587b432b52ef3f47b0f2b95d621ff70b70b3` based exactly on current #379 | Current Test `34573067654` is terminal SUCCESS. Relative to #379 the semantic delta remains only the two-line `TopFiles.svelte` accessibility repair and independently proves sequential Tab reachability, visible focus, PageDown scrolling, fragment continuity, normal/loading/empty/error/permission states, and inherited 375/768/1024 responsive behavior. |
| #390 — explicit cross-platform font fallback | current `0930488ccd1f4945332a6616334663c4b26f5706`; verified production predecessor `a801e91ea22e00c78af59e71acfe4f092f4c124a` | Predecessor Test `34583866853` is terminal SUCCESS and proves the actual production root font declaration plus real-Chrome multilingual fixture at 375/768/1024. Current head only adds decision-relevant JSDoc after a CodeRabbit docstring warning; production CSS is unchanged, but current Test `34585802287` remains in progress and predecessor GREEN does not transfer. This slice proves fallback declaration only, not shipped translation or native-shell parity. |
| #391/#392 — Localized Presentation Resource foundation | #392 current `4af86bd0adf572c37b6b5b43a3abf071c80e7eb7` based directly on #380 | Contract-first lineage introduced a real SQLite STRICT translation ledger and bounded version-aware lookup cache, then repaired append-only immutability with database triggers. ADR-0021 remains Proposed and explicitly separates presentation resources from filesystem ontology and active ADR-0020. Current head also adds `translationLedger.ts` to the mandatory 100% frontend production denominator without changing thresholds. Test `34586218305` is in progress; Windows, macOS and real llama.cpp jobs are already GREEN while the Ubuntu Test job is still nonterminal. This foundation does not claim a Tauri repository/loader, shipped screen-key migration, fallback policy, full eight-locale state parity, or native-shell acceptance. |
| #315 — canonical product/public docs | predecessor `94183e2ede1bea5413795bd50be0ef9cd7def147`, Test `34582588292` terminal SUCCESS; this full-file preservation update is newer | This projection replaces stale runtime statuses with terminal #386/#387/#389 evidence and records current #390/#392 localization foundations while retaining the unresolved native adapter/state-parity/native-shell gaps. Documentation confers no runtime, merge, or release authority and must settle independently. |

The verified OAuth path is `#264 → #344 → #339`, with #212 already normal-merged into #339 after adopting the tested shared-Test owner and obtaining source-present Windows process evidence. Shared Test ownership remains `#264 → #338 → consumers`; #338, #386, #387, and #389 now each have terminal exact-head Test evidence for their present lifecycle scope. #371 still shows a substantive owned-production Rust coverage gap rather than a runtime ambiguity. Current deletion safety retains #263's Proposed ADR-0020 fail-closed boundary.

UI Chrome/a11y work has current-head acceptance for its present scope. #375 supplied the original real 375px overflow RED; #379 localized nested internal overflow to long cleanup notice tokens, repaired those component-local constraints, and then repaired graceful browser shutdown/profile cleanup without retry masking. Exact #379 Test `34572975401` and exact #380 Test `34573067654` are terminal SUCCESS. #390 has a terminal-GREEN production predecessor for explicit CJK/system fallback but its current JSDoc-only head remains nonterminal. #392 now owns the versioned Localized Presentation Resource foundation with real SQLite append-only constraints and an admitted frontend-coverage denominator, but its current exact Test is also nonterminal. Neither slice supplies the native repository/resource loader, shipped screen-key migration, missing-message policy, complete eight-locale state/focus parity, or WebView2/WebKit evidence. Chrome GREEN is not localization or native-shell completeness.

Container/runtime work remains foundation-first. #383 remains harness-only. Canonical #386 and concrete adopters #387/#389 now have terminal exact-head Tests after ordinary/non-force adoption of shared-Test owner #338. That closes the prior evidence uncertainty for these three heads, but does not authorize protected-main integration while #264 governance/release gates and repository-wide 100% coverage remain unresolved. #384/#385 stay open until normal integration and the remaining caller-by-caller migration settle.

## Product invariants

1. Scan and classification are read-only until a separately authorized reclaim boundary is reached. Logical size, allocated size, filesystem identity, active use, materialization, provider state, ontology labels, and model advice are distinct evidence.
2. User-file removal is reversible and journaled. Permanent general user-file deletion is not a supported DiskSage action.
3. Regenerable-cache policy never grants irreversible authority merely from names, signatures, byte counts, or approval phrases. `--purge-proven-cache-trash` remains inspection-only and shipped execution fails closed when identity-bound permanent-delete capability is unavailable.
4. A successful mutation must consume the exact reviewed object. Repeating pathname checks is not sufficient authority for replacement or irreversible deletion. Unsupported platform primitives fail before mutation rather than falling back to delete-and-create or recursive pathname deletion.
5. Cloud copy, provider sync proof, local presence, local eviction, and source removal are separate transitions. Stale/incomplete provider evidence, quota/auth/headroom ambiguity, collisions, active use, placeholders, or identity gaps fail closed.
6. Models and ontology may classify, explain, or recommend but cannot override deterministic filesystem/deletion evidence or mutate DiskSage truth. Model-backed repository workflows consume `orchestrator/free` through contextual-orchestrator and do not pin a paid/direct provider locally.
7. Settings, receipts, approvals, and recovery records distinguish first-run absence from an existing unreadable or unsafe document. Failed replacement must preserve the previous complete value.
8. Crash/power-loss and durability claims are platform-specific and must name the primitive actually exercised. macOS `fsync` evidence is not described as stronger `F_FULLFSYNC` evidence.
9. Shared workflow behavior has one canonical owner. Source-absent conditional success is admission evidence, not runtime proof.
10. Release readiness belongs only to the exact protected lineage after all applicable deterministic, security, model/review, provenance, reproducibility, rollback, and human-governance gates pass. Predecessor success never transfers automatically.
11. Unix child-process cleanup must retain a live or waitable identity anchor until every process-group-directed signal is complete. A numeric PID/PGID observed before reaping is not durable mutation authority after the leader is consumed. With `waitid(..., WNOHANG | WNOWAIT)`, the returned child PID is the event/no-event discriminator; signal metadata alone is not used as readiness authority.
12. Browser E2E cleanup is part of acceptance evidence, not disposable teardown. The harness requests browser-level graceful shutdown, proves the launched browser exited, and only then deletes its profile; retry widening or ignored cleanup failure cannot convert a race into GREEN.
13. UI translation resources and filesystem/ontology labels are separate authorities. Locale text uses versioned screen keys/resource versions; ontology labels cannot silently become presentation-language storage or alter deterministic classification truth.

## Buyer-visible gaps

| Priority | Gap | Acceptance criterion |
| --- | --- | --- |
| P0 | Same-object mutation, Trash/recovery, and undo across Windows/Linux/macOS | Real-filesystem adversarial tests cover ancestor replacement, symlink/reparse/hardlink/FIFO substitution, mount and permission drift, staging replacement, partial mutation, crash/power-loss recovery, durable pending/terminal receipt, idempotent resume, and undo. Every success mutates the exact reviewed object; unsupported capability fails before mutation. Windows permanent cache deletion may advance only after same-handle `FILE_ID_INFO` identity plus handle-bound rename/disposition and recursive descendant evidence are proved. Linux/macOS remain unavailable until equivalent final-object binding or a separately accepted recoverable quarantine protocol exists. |
| P0 | Protected release and required-gate settlement | Canonical `.github` owners produce authenticated unchanged-head terminal CodeQL/model/review receipts for every applicable required workflow and independent human approval is satisfiable without self-approval. Then #264 can merge normally and release can proceed with immutable artifacts, SBOM, provenance, reproducibility evidence, and rollback. |
| P0 | Owned-production coverage at 100% | Exact-head frontend and Rust coverage reach 100% by executing meaningful production branches with realistic tests/fixtures. No threshold reduction, production exclusion, sample shrinking, or synthetic success. Current #371 `be858f7...` measures 81.527989% regions, 60.845956% branches, 69.555556% functions, and 82.003642% lines; `commands.rs` remains the largest uncovered production area. Commands requiring real filesystem/platform authority retain real adapter evidence rather than synthetic mocks. |
| P0 | Cache-Trash owner integration after ADR collision repair | #263 preserves Proposed ADR-0020 and its fail-closed irreversible boundary while semantically adopting current #264 through ordinary ancestry. #263 exact `04e72faa...` includes the complete ADR migration plus platform capability analysis and must be preserved by #362; the resulting integration head must reacquire applicable tests before downstream release claims. |
| P0 | Runtime process-group cleanup identity | #338 exact `ca1324d...`, Test `34577850939`, is terminal SUCCESS. #386 `a1d1cce...`, #387 `dd942d...`, and #389 `aa3ada3...` also have terminal exact-head Tests `34582209272`, `34582250033`, and `34582308667`. The reusable no-reap primitive and the container/File Provider adopters are therefore verified on their present heads. #384 closes only after normal container production integration; #385 remains open through protected integration and caller-by-caller migration. |
| P1 | Windows native private-publication/replacement parity | Use native handle-bound namespace authority, reject reparse drift, enforce private ACL-equivalent policy, define data/namespace flush behavior, and prove replacement plus partial-failure recovery on Windows. |
| P1 | macOS durability precision | Tests and documentation distinguish exercised `fsync` behavior from stronger device-flush semantics such as `F_FULLFSYNC`; recovery claims match actual primitives. |
| P1 | Provider-OAuth existing-document update | Prove exact-source-object replacement, cleanup, reparse/symlink resistance, previous-value preservation on failure, and platform-specific durability before enabling update. Bearer credential grammar hardening is no longer an open gap on #339 exact `1e3f434...`: Test `34506068789` is terminal SUCCESS, and downstream consumers must adopt that exact owner delta through verified ancestry rather than copying the validator. |
| P1 | Receipt-parent first-use bootstrap | Create/adopt the exact private receipt parent under race-resistant authority, prove identity/mode/durability, and expose approval only when execution can reacquire that authority. |
| P1 | Multilingual/native-shell material UI acceptance | Chrome responsive/state/a11y acceptance is GREEN on #379/#380. #390's production predecessor proves explicit CJK/system font fallback, while its current docstring-only head is still reacquiring exact acceptance. #392 provides a versioned append-only KO/EN/JA/ZH/VI/ES/DE/FR ledger/cache foundation and admits its production module into the existing 100% frontend denominator, but current exact acceptance is still running. Remaining delivery requires a native translation persistence/resource-loading adapter, stable screen-key migration of actual user-facing copy, explicit missing-message/fallback policy, realistic long DE/FR/ES/VI and CJK localized normal/loading/empty/error/permission states with keyboard/focus parity, and equivalent current-head Windows WebView2/macOS WebKit evidence or an explicit supported-platform contract. No ontology-label reuse as translation storage, silent truncation, fabricated locale completeness, or Chrome-only parity claim. |
| P1 | Near-duplicate cleanup evidence | Calibrated thresholds and provenance/quality/lineage evidence precede any reversible quarantine/Trash action; fresh identity/materialization/active-use preflight, durable receipt, and tested undo are required. |

## Standards and owner traceability

- POSIX pathname operations such as `openat()`/`renameat()`/`unlinkat()` bind resolution to directory descriptors but do not by themselves prove that the final name-selected mutation consumed the exact previously reviewed directory object. Linux `openat2()` adds strong `RESOLVE_*` path-resolution constraints and remains valuable for inspection/traversal, but does not independently satisfy the final-object deletion invariant.
- POSIX `waitid()` with `WNOWAIT` can observe an exited child while leaving it waitable. With `WNOHANG`, a zero returned `si_pid` denotes that no selected child is waitable; #386 uses `siginfo_t.si_pid()` as that discriminator and validates `SIGCHLD` only after a concrete child PID is present. Current exact Test `34582209272` is terminal SUCCESS, and #387/#389 consume the shared primitive rather than copying lifecycle code; their exact Tests `34582250033` and `34582308667` are also terminal SUCCESS.
- Windows same-object permanent-delete research is tied to native handle-oriented semantics. Microsoft documents `FILE_ID_INFO` volume serial + 128-bit file ID as a same-file identity check between open handles, and `SetFileInformationByHandle`/`FILE_DISPOSITION_INFO` as handle-based rename/delete primitives requiring appropriate access. DiskSage does not enable this path until recursive descendant, reparse, ACL, partial-progress, crash/recovery, and real NTFS/ReFS evidence is present.
- macOS persistence claims distinguish ordinary `fsync` evidence from stronger `F_FULLFSYNC` semantics. Portable POSIX name-based final mutation is not promoted to object-bound permanent-delete authority merely because ancestor traversal is descriptor-relative.
- OAuth bearer credentials sent in the `Authorization` header follow RFC 6750 §2.1 syntax. #339 exact `1e3f434...` enforces its admitted ASCII token alphabet/trailing-padding boundary before transport, with exact Test `34506068789` GREEN; downstream branches consume the verified owner delta rather than maintaining a divergent validator.
- Tauri 2's test module provides a MockRuntime path (`mock_builder`, `mock_context`, `WebviewWindowBuilder`, `get_ipc_response`) for invoking registered commands through IPC without a shipped desktop window; the module is gated by Tauri's `test` feature, and Tauri's own test example uses platform-local origins. DiskSage enables the feature only in dev/test dependency scope and preserves custom-command origin checks rather than granting remote IPC authority. This does not substitute for real platform/filesystem evidence where command authority depends on actual OS objects or native adapters.
- GitHub Actions evidence is exact-head evidence only when the relevant source exists and the workflow actually executes it. Failed-job rerun/carry-forward records must not be treated as proof that every reported job physically executed on the same attempt. Compound package scripts require original-invocation evidence before a failed workflow step is attributed to a nested test or browser phase. Source-absent conditional steps are admission evidence only. #338 exact `ca1324d...`, Test `34577850939`, is terminal SUCCESS for the source-present macOS provider process-contract lane, and #389 exact `aa3ada3...`, Test `34582308667`, independently proves that native adapter after the provider repair.
- Chrome browser-lifecycle evidence uses the browser-level DevTools endpoint exposed by `/json/version`; page-target CDP is interaction authority but not browser-process shutdown authority. `Browser.close` is requested before process-exit proof and profile removal, while bounded OS signals remain abnormal-startup/shutdown fallback rather than the primary success path. #379 exact `5b533ca...`, Test `34572975401`, is terminal SUCCESS and issue #388 is closed completed for this defect.
- For keyboard-operable overflow regions, DiskSage follows the W3C ACT expectation that scrollable content be reachable through sequential focus navigation; preserving a focusable scroll region is therefore an accessibility requirement, not an incidental tabindex side effect. #380 exact `6b5b587...`, Test `34573067654`, is terminal SUCCESS on the current #379 ancestry.
- Localized Presentation Resource authority is separate from filesystem ontology. Draft #392 uses a normalized SQLite STRICT ledger with immutable resource versions/screen keys/messages, admitted primary-language tags KO/EN/JA/ZH/VI/ES/DE/FR, and cache identity `(resource_version, locale, screen_key)`. ADR-0021 remains Proposed; schema/cache tests do not authorize shipped localization until native loading, screen-key rendering, missing-message policy, state parity, and native-shell evidence exist.
- Issue #170 owns broader deletion/recovery safety; issue #342 owns private-publication/exact-source replacement; issue #340 owns the remaining multilingual/font/native-shell material-UI acceptance; issue #391 owns the translation-resource foundation; issue #384 owns the concrete container process-group lifetime defect; issue #385 owns reusable Unix subprocess lifecycle migration. Issue #388 is closed after exact browser graceful-shutdown/profile-cleanup acceptance. Central CodeQL/model workflow defects remain canonical `.github` owner work rather than DiskSage leaf substitutes.
