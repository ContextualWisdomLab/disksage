# DiskSage product and technical gap baseline

The canonical product outcomes, supported capabilities, non-goals, and safety invariants are defined in the [DiskSage PRD](PRD.md). This file is the current implementation and ownership projection. Runtime receipts, exact filesystem evidence, released contracts, protected refs, and live GitHub governance remain more authoritative than this dated snapshot. The prior 2026-08-22 incident baseline remains preserved at [docs/archive/product-technical-gap-baseline-2026-08-22.md](archive/product-technical-gap-baseline-2026-08-22.md).

**Snapshot:** 2026-09-05 (Asia/Seoul)

**Protected main:** `0e90f9cebadbd7f59606baaec4ca1d2f178c899a` (`#343`, trigger-aware pull-request release lifecycle)

**Product boundary:** DiskSage owns Windows/Linux/macOS local-first disk-space inventory, filesystem classification/ontology, deletion safety, recovery, and platform adapters. Optional cloud, identity, ontology, LLM, security, and architecture capabilities consume released owner contracts through ports/ACLs and do not become DiskSage mutation authority.

**Evidence rule:** queued, pending, skipped-required, failed, cancelled, stale, predecessor, synthetic-only, model-only, author-only, or status-only evidence is non-passing. Read every base/head again before a readiness, merge, release, or inheritance claim.

## Current dependency and ownership map

| Lane | Exact head | Current owner relation | Status / next proof |
| --- | --- | --- | --- |
| PR #264 — release/Test foundation | `3e33229b4157a084a1985a1c7e952f98a64f25e3` | Directly on protected `main`; canonical #341/Test + release artifact namespace/rerun/verifier owner | Exact-head Test `33937972239` and Release `33937972244` are terminal success. Security Scan `33937972188`, SAST `33937972193`, and CodeQL `33937972182` remain queued, so no unchanged-head GREEN or merge claim exists. The two latest commits repair contract tests only. Ready-for-review is not merge authorization. |
| PR #344 — reusable private-publication foundation | `2a23a1d7de5b929a76b432c192d2b9e537fbbbdd` | Non-force descendant of exact #264; reusable local private-record authority owner | Five-file semantic owner set unchanged. Unix object-bound create-new/replace uses descriptor-relative mutation, real ancestor-replacement fixtures, owner-only mode admission, and containing-directory sync. Windows fails closed until native handle-relative parity exists. Exact-head Test `33938416475` is queued; Draft Release `33938416605` is skipped and non-passing. |
| PR #263 — cache-Trash fail-closed security | `d9b7fcf83bc0799fc9d53d0b739d87dfa42a54b6` | Non-force descendant of exact #264; issue #170 runtime owner | Thirteen cache/security semantic files preserved; current #264 contract-test repairs inherited through ancestry. Permanent cache-Trash deletion remains unavailable. Keep Draft until exact-head gates pass. |
| PR #315 — canonical product/public docs | resolve live on `codex/canonical-prd` | Documentation/public-surface owner downstream of #263 | This file is the canonical routing projection. Runtime authority stays with runtime owners. Every docs commit changes the exact head, so resolve and reacquire checks/review immediately before integration. |
| PR #339 — provider-OAuth domain authority | `42c89e253f07b4c0bfe4ea75c47bcaa790e95a85` | Non-force descendant of exact #344; connection-document/token/callback/credential owner | `save_connections()` consumes #344 object-bound replacement with `0600` and maps durability/authority failures into stable domain errors. Windows therefore fails closed. Exact-head Test `33938456647` is queued; Draft Release `33938456649` is skipped. Issue #342 remains open for deterministic domain-level ancestor-replacement acceptance and Windows handle-bound parity. |
| PR #212 — provider-OAuth/iCloud operational CLI | `72665604821994b1f396c57e7a0ee271bea76de1` | Non-force descendant of exact #339; CLI/process/platform-adapter owner | Thirteen-file CLI semantic delta remains isolated. Provider-domain and filesystem-authority source stay inherited. Exact-head Test `33938475817` is queued; Draft Release `33938475796` is skipped and non-passing. |
| PR #338 — Test path-filter owner | `eb255b92a0c63f44947a5d932694bf1df0678914` | Non-force descendant of exact #264 | Semantic delta remains exactly `.github/workflows/test.yml` plus `src/lib/testWorkflowPathFilterContract.test.ts`; current #264 contract-test repairs are inherited through ancestry. Fresh exact-head gates are required. |
| PR #337 — exact-head coverage owner | `7d9d5040941650bf8ed8678b15092cffc439415a` | Non-force descendant of exact #338 | Coverage/test/docs semantic boundary remains owned here. Exact 100% thresholds are unchanged; provider-OAuth coverage remains #339-owned; historical #156 evidence is adopted owner-by-owner. Fresh repository-wide hosted measurement is required. |
| PR #195 — Inventory privacy/async owner | `d04f97c351042d185a475b519537f4da6cf153f6` | Non-force descendant of exact #264 | Exactly four Inventory semantic files remain the owner boundary; release/Test source is inherited. Fresh exact-head gates required. |
| PR #190 — Settings persistence owner | `dc02de371d7535bee0d2730c89314c876885a12f` | Non-force descendant of exact #264 | Six Settings/Tauri files remain the semantic boundary. Existing same-directory replacement evidence does not claim containing-directory power-loss durability. Fresh exact-head gates required. |
| PR #199 — Git-worktree feedback/recovery UX | `95d8ded4a61fe0903459d9cbe5cd58100c067fbe` | Non-force descendant of exact #264 | Three UI/privacy files remain isolated. Git retention, approval, mutation, journal, and filesystem authority stay outside this UI owner. Fresh exact-head gates required. |
| PR #202 → #203 — scan/navigation → TopFiles a11y | `060229015b4b81bb1d574a6827c8fb8c69fd7cdc` → `a0b3a9a0e465c0c24748b24e26e37dc0013a30a9` | #202 non-force descendant of exact #264; #203 non-force descendant of exact #202 | Issue #340 still requires real-browser normal/empty/loading/error, Tab/focus/scroll, fragment continuity, visible focus, and responsive long-path evidence. SSR is not browser interaction proof. |
| PR #313 — exact-photo duplicate audit | `94a07ca11815312e5fd44f7bc443cca6de09187e` | Non-force descendant of exact #264 | Exact decoded-PNG evidence grants no cleanup authority. Near-duplicate calibration, customer tie resolution, fresh execution preflight, reversible quarantine/Trash, durable journal, and undo remain buyer gaps. Fresh exact-head gates required. |
| PR #314 → #334 — native temp foundation → ontology-backed planning | `cc9c4f7400813c913cee79c5f317c9581a41acde` → `ef9f0d3819ed765e5de6742005da09e33479c1e3` | #314 non-force descendant of exact #264; #334 non-force descendant of exact #314 | Preserve real successful mutation evidence, but require same-object recovery/undo and platform-specific destructive/recovery acceptance before commercial completion. #334's additional platform Test jobs remain a legitimate leaf semantic delta. |
| PR #257 — Svelte 5.56.10 dependency update | `4ead53a9f4b2ff605243aef80c65314aff777325` | Non-force clean restack on exact #264 | Only `package.json` and `package-lock.json` remain semantic delta. The previous Dependabot base was 24 commits behind current #264; current release/Test files are inherited rather than overwritten. Fresh dependency/Test/security gates required. |
| PR #267 — container orphan reclaim | `43f2968a7175f9dee866b3d50d503a04f73c1f03` | Large mixed historical line on an old #264 base | 165 changed files / 524 commits make blind tree-preserving restack unsafe. Classify runtime, UI, docs, and release/Test overlaps by canonical owner before adopting current #264. Keep Draft; do not close or transfer predecessor checks. |
| PR #303 — provider cache reclaim | `56b68da6420a052e0e1a9f8fa4313394f9be1735` | Non-force descendant of exact #264; provider-cache runtime/ADR/API/test owner | Commercial Rust/Tauri/TypeScript planning now agrees on the facade-owned Trash-only schema: `trash_approval_phrase` is exposed, `exact_approval_phrase` is absent, and the TypeScript invoke no longer sends a redundant mode key. Test-only RED `44107488869850df6b5d67810182618216ad961a` made the stale payload observable; production `112988abfc9bbcd8ccc7e5945cbdea636146392f` repaired both TypeScript mismatches; ADR head `56b68da...` records the boundary. Historical irreversible plan/mode/result/executor remain crate-private repair evidence. Exact-head Test `33954344439` is queued and Draft Release `33954344472` is skipped, so no GREEN claim exists. |
| PR #318 — inconclusive provider/reclaim evidence presentation | `c3d405212b6095498b40a4b1711681938a8f0bde` | Historical mixed provider/runtime/UI/docs line on old #264 base | Sixteen semantic files span multiple owners. Classify overlaps before non-force restack rather than preserving a historical tree that could overwrite current owners. Keep Draft. |
| PR #341 — hosted runner/Vitest predecessor | `bbe5cc2375a7f83a4a174a2f799dee904f2c9e4a` | Historical predecessor; valid post-checkpoint release edits individually classified against #264 | Returned to Draft. Valid release semantics are present or superseded on #264. Do not merge #341 around #264; close only after #264 integrates and full inheritance is reverified. |
| PR #156 — historical coverage evidence donor | `a3803fced0bdbb4f8eb9d19d07a623b1422e5204` | Historical donor, not a direct merge candidate | Keep open until every useful unique test/fixture/contract/evidence delta has a verified current owner or a technical rejection. |

A branch can be mechanically mergeable while still stale relative to its canonical owner; mergeability does not convert predecessor evidence into current proof. The repaired active OAuth chain is `#264 → #344 → #339 → #212`. The repaired coverage chain is `#264 → #338 → #337`, the UI chain is `#264 → #202 → #203`, and the native-temp chain is `#264 → #314 → #334`. Large historical lines with mixed ownership are classified before restack rather than allowed to overwrite current owner blobs.

## Current product contract

1. Scan and classification are read-only until a separately authorized reclaim boundary is reached. Logical size, allocated size, object identity, active use, materialization, provider state, ontology labels, and model advice remain distinct evidence.
2. User-file removal is reversible and journaled. Permanent user-file deletion is not a supported general DiskSage action.
3. The ADR-0002 regenerable-cache policy is a narrow reversible exception to repeated human approval only for fixed, evidenced cache classes. Unrelated children, active/ambiguous candidates, user files, provider data, and irreversible deletion remain outside that authority.
4. `--purge-proven-cache-trash` remains inspection-only. An execution request must fail before journal or filesystem mutation until the proposed irreversible contract has full descendant identity, fresh approval, replacement-race resistance, durable recovery, and platform evidence. Historical #303 permanent-purge helpers remain crate-private repair evidence. The external Rust facade and TypeScript client own a matching Trash-only plan/mode/result contract; Rust/Tauri/TypeScript plan schemas omit irreversible approval, the Tauri execution command accepts no mode argument, the TypeScript invoke sends no mode key, and the headless CLI rejects permanent purge before executor work.
5. Cloud copy, remote-sync proof, local presence, local eviction, and source removal are separate state transitions. Stale/incomplete provider evidence, quota/auth/headroom ambiguity, collisions, active use, placeholders, or identity gaps fail closed.
6. Models and ontology may classify, explain, or recommend. They cannot override deterministic filesystem/deletion evidence or mutate DiskSage truth.
7. Source files, open PRs, local tests, and development measurements are not immutable release evidence.
8. Settings persistence distinguishes first-run absence from an existing unreadable or unsafe document. Failed replacement must preserve the previous complete value; stronger crash/power-loss durability requires explicit platform evidence.

## Buyer-visible gaps

| Priority | Gap | Current evidence | Acceptance criterion |
| --- | --- | --- | --- |
| P0 | Filesystem mutation must remain safe under ancestor replacement, symlink/reparse/hardlink substitution, partial failure, and crash recovery. | #344 carries real Unix object-bound create-new + atomic-replace fixtures and rejects non-private modes. #339 consumes that mutation owner for OAuth publication. #303 removes the historical irreversible plan/mode/result/executor from external Rust/Tauri/TypeScript authority and keeps it crate-private as repair evidence only. | Real Windows/Linux/macOS fixtures bind the exact reviewed object from admission through mutation, prevent redirected publication/deletion, persist recoverable state durably, and prove undo/recovery without unrelated-data loss. Reusable foundation and each domain consumer require their own evidence; permanent deletion remains unavailable until the internal repair helper is replaced by canonical identity-bound deletion/recovery authority. |
| P0 | Release/Test roots and descendants must share one current protected lineage before any current-evidence claim. | #264 is on protected-main #343 at `3e33229...`; mapped active descendants and repaired #303 are non-force descendants of that exact foundation. #264 Test and Release are terminal success, while Security/SAST/CodeQL remain queued. Mixed historical lines #267/#318 still need owner classification rather than blind restack. | #264 lands normally only after unchanged-head gates and independent review; every descendant adopts that exact foundation without force and then proves its own unchanged-head gates. No predecessor-evidence transfer. |
| P1 | Provider-OAuth connection documents lack completed cross-platform stable filesystem mutation authority and post-rename durability evidence. | #344 implements Unix descriptor-bound replacement, directory sync, and private-mode admission. #339 routes publication through it and exposes stable domain failures. Windows remains deliberately fail-closed, and domain-level adversarial race acceptance is still missing. | #339 domain fixtures replace the visible parent before temp creation and between temp sync and publication and cover post-publish uncertainty; Windows uses validated handle-bound authority for temp creation and final replacement; macOS states and tests the persistence guarantee actually used. |
| P1 | Exact photo audit does not yet justify near-duplicate cleanup. | #313 separates decoded identity, dimensions, metadata, bounded allocations, and audit failure, but is audit-only. | Calibrated perceptual thresholds on documented data; provenance/quality/lineage kept separate; customer resolves evidence ties; fresh identity/materialization/active-use preflight; reversible quarantine/Trash with durable receipt and tested undo. |
| P1 | TopFiles interaction/accessibility lacks real-browser evidence. | #202/#203 provide page/component contracts; issue #340 remains open after ancestry repair. | Current-head browser E2E covers normal/loading/empty/error/permission states where applicable, keyboard traversal and scroll, fragment continuity, focus visibility, and responsive long-path overflow. |
| P1 | Settings and other local JSON publication paths must distinguish atomic replacement from durable publication. | #190 has real replacement/failure fixtures but does not claim directory durability. #344 provides a reusable Unix object-bound pattern; adoption remains owner-specific rather than assumed. | Each mutable local document chooses and documents the required durability guarantee, uses stable authority where replacement races matter, and has platform-specific crash/failure evidence. |
| P2 | Repository-wide owned-production documentation/test/edge-case coverage is not proven at 100% on one integrated protected head. | #337 now inherits current #338 and current #264 while retaining selected realistic #156 donors. A fresh repository-wide hosted measurement is still required. | Non-vacuous exact 100% region/statement-equivalent, branch, function, line, public-doc, and edge-case evidence on one unchanged integrated head without exclusions or threshold weakening. |

## DDD and owner contracts

DiskSage domain truth stays in DiskSage. Bounded contexts keep filesystem inventory/classification, reclaim/deletion safety, recovery, provider-local evidence, and platform adapters explicit in code/API/test names and invariants. Transactions and shared kernels remain minimal; cross-service SQL, mutable sibling PR-head dependencies, and copied owner source are defects.

`.github`, `enterprise-architecture-core`, `context-graph-contracts`, `ConceptWeave`, `semantic-data-portal`, `contextual-orchestrator`, `noema`, `keyverse`, `EgressWeave`, `OriginWeave`, `pingora-gateway`, `quarantine-sandbox-runtime`, `appguardrail`, `wardnet`, `pg-llm-batch`, `EmbedRelay`, `fast-mlsirm`, `TEPP`, `RankWeave`, `ThreadWeave`, `inkspan`, `DiagramWeave`, and `mhtml-etl-gateway` remain canonical owners of their shared capabilities. DiskSage consumes only released/versioned contracts through explicit adapters/ACLs.

For model-backed work, `contextual-orchestrator` remains the routing/provider boundary. Deterministic safety, merge, coverage, security, recovery, and release gates do not depend on model judgment.

## Quality, security, accessibility, and release acceptance

Destructive/recovery acceptance uses real filesystem/path/symlink/hardlink/mount/permission/race scenarios; synthetic data is unit-test support only. Material UI acceptance requires rendered normal/loading/empty/error/permission states plus real browser keyboard/focus/scroll evidence when interaction semantics matter. Rust remains the preferred hot-path, numerical, performance, and security runtime; Python requires a bounded validation role and explicit removal condition where used.

A performance claim such as buyer-path p95 ≤20 ms is valid only for a defined, measured path and environment. Sampling away slow cases, shrinking the workload, or relying on unrealistic warm-cache state does not satisfy the claim. CSAP/SOC 2 language is evidence-readiness unless an actual certification or attestation exists.

AGENTS.md, CLAUDE.md, PRD/TRD, ARCHITECTURE, Context Map/Ubiquitous Language, ADRs, UML/ERD or truthful logical data model, UX, SECURITY/THREAT_MODEL, TEST_STRATEGY, OPERABILITY/recovery, TRACEABILITY/doctoring, README, CHANGELOG, and this baseline must remain code-current. A GitHub Pages claim requires an actual published site check.

A release is permitted only from one exact integrated protected head after applicable Test, security/SAST, exact coverage/docstrings, package/build, SBOM, provenance, reproducibility, review, rollback/recovery, and accessibility evidence has been verified. Version, CHANGELOG, tag, package, and immutable release must describe the same protected artifact set. Pending or predecessor evidence is never release readiness.
