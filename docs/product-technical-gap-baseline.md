# DiskSage product and technical gap baseline

The canonical product outcomes, supported capabilities, non-goals, and safety invariants are defined in the [DiskSage PRD](PRD.md). This file is the current implementation and ownership projection. Runtime receipts, exact filesystem evidence, released contracts, protected refs, and live GitHub governance remain more authoritative than this dated snapshot. The prior 2026-08-22 incident baseline remains preserved at [docs/archive/product-technical-gap-baseline-2026-08-22.md](archive/product-technical-gap-baseline-2026-08-22.md).

**Snapshot:** 2026-09-05 (Asia/Seoul)

**Protected main:** `0e90f9cebadbd7f59606baaec4ca1d2f178c899a` (`#343`, trigger-aware pull-request release lifecycle)

**Product boundary:** DiskSage owns Windows/Linux/macOS local-first disk-space inventory, filesystem classification/ontology, deletion safety, recovery, and platform adapters. Optional cloud, identity, ontology, LLM, security, and architecture capabilities consume released owner contracts through ports/ACLs and do not become DiskSage mutation authority.

**Evidence rule:** queued, pending, skipped-required, failed, cancelled, stale, predecessor, synthetic-only, model-only, author-only, or status-only evidence is non-passing. Read every base/head again before a readiness, merge, release, or inheritance claim.

## Current dependency and ownership map

| Lane | Exact head | Current owner relation | Status / next proof |
| --- | --- | --- | --- |
| PR #264 — release/Test foundation | `5c4eedeb6a2f48ddf9030c18d970fedf85dda2ac` | Directly on protected `main`; canonical #341/Test + release artifact namespace/rerun/verifier owner | Ready-for-review only to exercise the `ready_for_review` release trigger, not merge authorization. Exact-head Test, Security, SAST, CodeQL and central gates still need terminal success on the unchanged head; Release has a current success run but does not satisfy the other gates. |
| PR #344 — reusable private-publication foundation | `aebaab1070d3900b286aba4e3e2111c457931030` | Non-force descendant of exact #264; reusable local private-record authority owner | Carries the verified current-lineage #228 create-new object-binding delta plus a Unix atomic-replace primitive. Real fixtures replace the visible parent before temp creation, after temp sync, and after rename; mutation remains descriptor-relative and post-rename directory sync is explicit. Windows intentionally fails closed until native handle-relative parity exists. Exact-head Test/security/review remains pending; this is prerequisite code, not shipped proof. |
| PR #263 — cache-Trash fail-closed security | `f25f51a2d8c3f6dc9fffcea28e53ea7f9efbd64b` | Non-force restacked on exact #264; issue #170 runtime owner | The 13-file cache/security semantic delta is preserved while current release lifecycle/provenance is inherited through ancestry. Keep Draft until #264 integrates and exact-head gates pass. Permanent cache-Trash deletion remains unavailable. |
| PR #315 — canonical product/public docs | resolve live on `codex/canonical-prd` | Non-force restacked on exact #263; documentation/public-surface owner only | This file is the canonical current routing projection. Every docs commit changes the exact head, so re-resolve and reacquire checks/review before integration. Runtime authority stays with runtime owners. |
| PR #339 — provider-OAuth domain authority | `c05b5daaa0ce2297402978407afa20b45702185a` | Non-force restacked on exact #344, which inherits #264; connection-document/token/callback/credential owner | Draft and mergeable. #344 now supplies the Unix object-bound replacement primitive, but #339 production `save_connections()` has not consumed it: pathname temp creation/cleanup/rename and missing explicit containing-directory sync remain the domain RED. Issue #342 stays open until the domain consumer, real domain-level ancestor-replacement evidence, and Windows handle-bound parity are complete. |
| PR #212 — provider-OAuth/iCloud operational CLI | `9dbf81a5eb373114bde7084581038d74ce0fbf6c` | Non-force restacked on exact #339; CLI/process/platform-adapter owner | The 13-file CLI delta remains isolated; #344/#342 domain source is inherited through #339, not copied. Keep Draft and restack again after any #339 causal repair. |
| PR #338 — Test path-filter owner | `75232439cb47300abd86b21a429225b416dc6aa8` | Non-force restacked on exact #264 | Fresh #264→#338 is descendant-only with merge base exactly current #264; semantic delta remains exactly `.github/workflows/test.yml` plus `src/lib/testWorkflowPathFilterContract.test.ts`. Reacquire exact-head gates before integration. |
| PR #337 — exact-head coverage owner | `0126d9d51f67178cf461f7661d2708f8168f7677` | Non-force restacked on exact #338 after parent-first #338 repair | Fresh #338→#337 is descendant-only with merge base exactly current #338 and the coverage/test/docs semantic boundary remains 20 files. Exact 100% thresholds remain unchanged; provider-OAuth coverage remains #339-owned; historical #156 evidence is adopted only owner-by-owner. |
| PR #195 — Inventory privacy/async owner | `07de8ddc73ab2383d19d7536e7b9643df51e2fa6` | Non-force restacked on exact #264 | Fresh #264→#195 is `behind_by=0` with merge base exactly current #264 and exactly four Inventory semantic files. Release/Test source remains inherited. |
| PR #190 — Settings persistence owner | `fe4460c960181031f9cba352e683762ed493da3b` | Non-force restacked on exact #264 | Six Settings/Tauri files remain the semantic boundary. Existing same-directory replacement evidence does not claim containing-directory power-loss durability. |
| PR #199 — Git-worktree feedback/recovery UX | `0b508c087101fba0174cba8ebcb77981178d0f13` | Non-force restacked on exact #264 | Three UI/privacy files remain isolated. Git retention, approval, mutation, journal, and filesystem authority stay outside this UI owner. |
| PR #202 → #203 — scan/navigation → TopFiles a11y | `0369a80e0860d0d06bddb5e4e8916704b618eb01` → `b6aab1d01b419b36aff251186b6f277867766de7` | Parent-first non-force restack: #202 on exact #264, #203 on exact #202 | Issue #340 still requires real-browser normal/empty/loading/error, Tab/focus/scroll, fragment continuity, visible focus, and responsive long-path evidence. SSR is not browser interaction proof. |
| PR #313 — exact-photo duplicate audit | `2448e06077bdad38986f4fadb7cc027f0ffe633f` | Non-force restacked on exact #264; audit-only photo runtime/ADR | Exact decoded-PNG evidence grants no cleanup authority. Near-duplicate calibration, customer tie resolution, fresh execution preflight, reversible quarantine/Trash, durable journal, and undo remain buyer gaps. |
| PR #314 → #334 — native temp foundation → ontology-backed planning | `3e2d3fa669198703120744886e9094e5e37ce6f9` → `d498a52c4470e0d714bf36fa03adab52c15eabcd` | Parent-first non-force restack: #314 on exact #264, #334 on exact #314 | Preserve real successful mutation evidence, but require same-object recovery/undo and platform-specific destructive/recovery acceptance before commercial completion. #334's additional platform Test jobs remain a legitimate leaf semantic delta. |
| PR #341 — hosted runner/Vitest predecessor | `bbe5cc2375a7f83a4a174a2f799dee904f2c9e4a` | Historical predecessor; valid post-checkpoint release edits individually classified against #264 | Returned to Draft. Its three post-checkpoint commits repair `windows-2022`, PR release supersession, and explicit-rerun cancellation; those valid semantics are already present or superseded on #264, which remains canonical. Do not merge #341 around #264. Close only after #264 integrates and full inheritance is reverified. |
| PR #156 — historical coverage evidence donor | `a3803fced0bdbb4f8eb9d19d07a623b1422e5204` | Historical donor, not a direct merge candidate | Body refreshed to current coverage/runtime owners. Keep open until every useful unique test/fixture/contract/evidence delta has a verified current owner or a technical rejection. |

A branch can be mechanically mergeable while still being stale relative to its canonical owner; mergeability does not convert predecessor evidence into current proof. The active OAuth chain is now `#264 → #344 → #339 → #212`: #344 owns reusable filesystem authority, #339 owns provider-OAuth domain semantics, and #212 owns the shipped process/platform adapter. Every changed head needs fresh checks and review evidence.

## Current product contract

1. Scan and classification are read-only until a separately authorized reclaim boundary is reached. Logical size, allocated size, object identity, active use, materialization, provider state, ontology labels, and model advice remain distinct evidence.
2. User-file removal is reversible and journaled. Permanent user-file deletion is not a supported general DiskSage action.
3. The ADR-0002 regenerable-cache policy is a narrow reversible exception to repeated human approval only for fixed, evidenced cache classes. Unrelated children, active/ambiguous candidates, user files, provider data, and irreversible deletion remain outside that authority.
4. `--purge-proven-cache-trash` remains inspection-only. An execution request must fail before journal or filesystem mutation until the proposed irreversible contract has full descendant identity, fresh approval, replacement-race resistance, durable recovery, and platform evidence.
5. Cloud copy, remote-sync proof, local presence, local eviction, and source removal are separate state transitions. Stale/incomplete provider evidence, quota/auth/headroom ambiguity, collisions, active use, placeholders, or identity gaps fail closed.
6. Models and ontology may classify, explain, or recommend. They cannot override deterministic filesystem/deletion evidence or mutate DiskSage truth.
7. Source files, open PRs, local tests, and development measurements are not immutable release evidence.
8. Settings persistence distinguishes first-run absence from an existing unreadable or unsafe document. Failed replacement must preserve the previous complete value; stronger crash/power-loss durability requires explicit platform evidence.

## Buyer-visible gaps

| Priority | Gap | Current evidence | Acceptance criterion |
| --- | --- | --- | --- |
| P0 | Filesystem mutation must remain safe under ancestor replacement, symlink/reparse/hardlink substitution, partial failure, and crash recovery. | #344 now carries a real Unix object-bound create-new + atomic-replace foundation with deterministic ancestor-replacement fixtures; #339/#342 still has the domain consumer RED; #314/#334 and other reclaim lanes carry real filesystem evidence of varying completeness. | Real Windows/Linux/macOS fixtures bind the exact reviewed object from admission through mutation, prevent redirected publication/deletion, persist recoverable state durably, and prove undo/recovery without unrelated-data loss. Reusable foundation evidence and domain-consumer evidence are both required. |
| P0 | Release/Test roots and descendants must share one current protected lineage before any current-evidence claim. | #264 adopted protected-main #343. The OAuth chain is now #264→#344→#339→#212; other mapped chains remain on #264. #341's later edits were classified rather than blindly inherited. | #264 lands normally after unchanged-head gates/review; descendants then prove their own unchanged-head gates. No force push or predecessor-evidence transfer. |
| P1 | Provider-OAuth connection documents lack completed stable filesystem mutation authority and cross-platform post-rename durability evidence. | #344 implements the Unix descriptor-bound replacement primitive and directory sync, while #339's source-level RED still detects pathname `save_connections()` publication. Windows parity is deliberately unavailable rather than falling back to pathname mutation. | #339 consumes stable directory authority for temporary creation, cleanup, final replacement, and containing-directory persistence; domain fixtures replace the visible parent before temp creation and between temp sync and publication; Windows uses validated handle-bound authority; macOS states and tests the persistence guarantee actually used. |
| P1 | Exact photo audit does not yet justify near-duplicate cleanup. | #313 separates decoded identity, dimensions, metadata, bounded allocations, and audit failure, but is audit-only. | Calibrated perceptual thresholds on documented data; provenance/quality/lineage kept separate; customer resolves evidence ties; fresh identity/materialization/active-use preflight; reversible quarantine/Trash with durable receipt and tested undo. |
| P1 | TopFiles interaction/accessibility lacks real-browser evidence. | #202/#203 provide page/component contracts; issue #340 remains open after ancestry repair. | Current-head browser E2E covers normal/loading/empty/error/permission states where applicable, keyboard traversal and scroll, fragment continuity, focus visibility, and responsive long-path overflow. |
| P1 | Settings and other local JSON publication paths must distinguish atomic replacement from durable publication. | #190 has real replacement/failure fixtures but does not claim directory durability. #344 now provides a reusable Unix object-bound pattern; adoption remains owner-specific rather than assumed. | Each mutable local document chooses and documents the required durability guarantee, uses stable authority where replacement races matter, and has platform-specific crash/failure evidence. |
| P2 | Repository-wide owned-production documentation/test/edge-case coverage is not proven at 100% on one integrated protected head. | #337 is parent-first restacked through exact #338/#264 and carries current-stack coverage infrastructure plus selected realistic #156 donors; its new head still needs hosted compile/runtime and a fresh repository-wide measurement. | Non-vacuous exact 100% region/statement-equivalent, branch, function, line, public-doc, and edge-case evidence on one unchanged integrated head without exclusions or threshold weakening. |

## DDD and owner contracts

DiskSage domain truth stays in DiskSage. Bounded contexts should keep filesystem inventory/classification, reclaim/deletion safety, recovery, provider-local evidence, and platform adapters explicit in code/API/test names and invariants. Transactions and shared kernels remain minimal; cross-service SQL, mutable sibling PR-head dependencies, and copied owner source are defects.

`.github`, `enterprise-architecture-core`, `context-graph-contracts`, `ConceptWeave`, `semantic-data-portal`, `contextual-orchestrator`, `noema`, `keyverse`, `EgressWeave`, `OriginWeave`, `pingora-gateway`, `quarantine-sandbox-runtime`, `appguardrail`, `wardnet`, `pg-llm-batch`, `EmbedRelay`, `fast-mlsirm`, `TEPP`, `RankWeave`, `ThreadWeave`, `inkspan`, `DiagramWeave`, and `mhtml-etl-gateway` remain canonical owners of their shared capabilities. DiskSage consumes only released/versioned contracts through explicit adapters/ACLs.

For model-backed work, `contextual-orchestrator` remains the routing/provider boundary. Deterministic safety, merge, coverage, security, recovery, and release gates do not depend on model judgment.

## Quality, security, accessibility, and release acceptance

Destructive/recovery acceptance uses real filesystem/path/symlink/hardlink/mount/permission/race scenarios; synthetic data is unit-test support only. Material UI acceptance requires rendered normal/loading/empty/error/permission states plus real browser keyboard/focus/scroll evidence when interaction semantics matter. Rust remains the preferred hot-path, numerical, performance, and security runtime; Python requires a bounded validation role and explicit removal condition where used.

A performance claim such as buyer-path p95 ≤20 ms is valid only for a defined, measured path and environment. Sampling away slow cases, shrinking the workload, or relying on unrealistic warm-cache state does not satisfy the claim. CSAP/SOC 2 language is evidence-readiness unless an actual certification or attestation exists.

AGENTS.md, CLAUDE.md, PRD/TRD, ARCHITECTURE, Context Map/Ubiquitous Language, ADRs, UML/ERD or truthful logical data model, UX, SECURITY/THREAT_MODEL, TEST_STRATEGY, OPERABILITY/recovery, TRACEABILITY/doctoring, README, CHANGELOG, and this baseline must remain code-current. A GitHub Pages claim requires an actual published site check.

A release is permitted only from one exact integrated protected head after applicable Test, security/SAST, exact coverage/docstrings, package/build, SBOM, provenance, reproducibility, review, rollback/recovery, and accessibility evidence has been verified. Version, CHANGELOG, tag, package, and immutable release must describe the same protected artifact set. Pending or predecessor evidence is never release readiness.