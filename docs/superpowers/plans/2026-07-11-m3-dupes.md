# DiskSage M3: 중복 파일 탐지 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 크기 → 부분 해시 → BLAKE3 전체 해시의 3단계 파이프라인으로 중복 파일 그룹을 찾아 검토·삭제(safety 계층 경유) UI에 연동한다.

**Architecture:** 새 `dupes` 모듈이 파일 목록을 (1) 크기로 그룹핑 → (2) 각 그룹에서 앞 4KB 부분 해시로 재그룹핑 → (3) 후보만 BLAKE3 전체 해시로 확정한다. 단계마다 앞 단계 통과분만 다음 I/O로 넘겨 불필요한 해시를 피한다. 커맨드 래퍼는 `#[cfg(not(coverage))]`, 순수 로직은 100% 라인 커버리지. 삭제는 기존 `safety::trash_delete` 경로를 그대로 재사용(신규 삭제 코드 없음).

**Tech Stack:** 기존 스택 + `blake3` 크레이트(전체 해시). 부분 해시도 blake3(첫 4KB)로 통일.

## Global Constraints

- 스펙: `docs/superpowers/specs/2026-07-10-disksage-design.md` §4(dupes 모듈)/§9 — 충돌 시 스펙 우선
- **안전 불변식 (M2에서 확립, 유지)**: 삭제는 오직 `safety::trash_delete` 경유. dupes 모듈은 후보 그룹만 생산하고 삭제하지 않는다. UI는 그룹 내 "보존 1개 + 나머지 삭제" 선택을 검토·확인 후에만 실행
- **조직 CI 게이트**: 리눅스 러너에서 `cargo llvm-cov --all-features --fail-under-lines 100`. Tauri command 래퍼는 `#[cfg(not(coverage))]`, 순수 로직은 테스트로 100%. `cargo-llvm-cov`는 `--cfg coverage`를 세팅하므로 헤드리스 불가 래퍼는 커버리지 빌드에서 사라져야 함. JS는 `npm run coverage` 4지표 100 (새 순수 .ts 모듈 추가 시 vitest.config.ts include + 테스트)
- 에러 arm은 한 줄 let-else(라인 커버리지 안정). cfg(unix) 폴트 인젝션 패턴은 scanner.rs/safety.rs 테스트 참조. 심링크는 모든 순회에서 제외(scanner::keep_entry `pub(crate)` 재사용)
- 커밋: conventional commits + 트레일러 `Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>`
- **원격 main 직접 push 불가** — 브랜치 `feat/m3-dupes`, 마지막에 PR (스쿼시 머지)
- `cargo`는 새 셸 PATH에 없음: bash `export PATH="$HOME/.cargo/bin:$PATH"` / PowerShell `& "$env:USERPROFILE\.cargo\bin\cargo.exe"`. cargo test 타임아웃 600000ms
- 테스트가 만든 파일은 tempdir 픽스처로 자동 정리. 실제 삭제는 M3 순수 로직 테스트에 등장하지 않는다 (dupes는 삭제하지 않음)

---

### Task 1: `dupes` — 크기 그룹핑

**Files:**
- Create: `src-tauri/src/dupes.rs`
- Modify: `src-tauri/src/lib.rs` (`mod dupes;` — sibling `#[cfg_attr(coverage, allow(dead_code))]` 패턴)
- Test: `src-tauri/src/dupes.rs` 내 tests

**Interfaces:**
- Consumes: 없음
- Produces:
  - `dupes::FileEntry { path: PathBuf, size: u64 }` (파일 수집 결과 — 상위에서 주입)
  - `dupes::group_by_size(files: Vec<FileEntry>) -> Vec<Vec<FileEntry>>` — 크기 2개 이상만, 각 그룹 크기 내림차순 정렬, size 0 제외

- [ ] **Step 1: 실패하는 테스트 작성**

`src-tauri/src/dupes.rs` 생성, 하단에:

```rust
#[cfg(test)]
mod tests {
    use super::*;
    use std::path::PathBuf;

    fn fe(p: &str, size: u64) -> FileEntry {
        FileEntry { path: PathBuf::from(p), size }
    }

    #[test]
    fn groups_only_collisions_excludes_singletons_and_zero() {
        let files = vec![
            fe("/a", 100),
            fe("/b", 100),
            fe("/c", 50),   // 단독 — 제외
            fe("/d", 0),    // 0바이트 — 제외
            fe("/e", 0),    // 0바이트 — 제외
            fe("/f", 100),
        ];
        let groups = group_by_size(files);
        // 100바이트 그룹 하나만 (3개), 정렬은 크기 내림차순인데 동일 크기라 그룹 내부는 유지
        assert_eq!(groups.len(), 1);
        assert_eq!(groups[0].len(), 3);
        assert!(groups[0].iter().all(|f| f.size == 100));
    }

    #[test]
    fn multiple_size_groups_sorted_desc() {
        let files = vec![
            fe("/a", 10), fe("/b", 10),
            fe("/c", 999), fe("/d", 999),
        ];
        let groups = group_by_size(files);
        assert_eq!(groups.len(), 2);
        // 그룹은 크기 내림차순: 999 그룹이 먼저
        assert_eq!(groups[0][0].size, 999);
        assert_eq!(groups[1][0].size, 10);
    }

    #[test]
    fn empty_input_is_empty() {
        assert!(group_by_size(Vec::new()).is_empty());
    }
}
```

- [ ] **Step 2: 컴파일 실패 확인**

`lib.rs`에 `#[cfg_attr(coverage, allow(dead_code))] mod dupes;` 추가 후:

Run: `cd src-tauri; cargo test dupes`
Expected: COMPILE ERROR — `FileEntry`/`group_by_size` not found

- [ ] **Step 3: 구현**

`src-tauri/src/dupes.rs` 상단:

```rust
use std::collections::HashMap;
use std::path::PathBuf;

#[derive(Debug, Clone)]
pub struct FileEntry {
    pub path: PathBuf,
    pub size: u64,
}

/// 1단계: 크기가 같은 파일만 중복 후보. size 0과 단독 크기는 제외.
/// 반환 그룹은 크기 내림차순 (큰 중복부터 사용자에게 보여주기 위함).
pub fn group_by_size(files: Vec<FileEntry>) -> Vec<Vec<FileEntry>> {
    let mut by_size: HashMap<u64, Vec<FileEntry>> = HashMap::new();
    for f in files {
        if f.size == 0 {
            continue;
        }
        by_size.entry(f.size).or_default().push(f);
    }
    let mut groups: Vec<Vec<FileEntry>> =
        by_size.into_iter().filter(|(_, v)| v.len() >= 2).map(|(_, v)| v).collect();
    groups.sort_by(|a, b| b[0].size.cmp(&a[0].size));
    groups
}
```

- [ ] **Step 4: 통과 확인 + Commit**

Run: `cd src-tauri; cargo test dupes`
Expected: 3 tests PASS

```powershell
git add src-tauri
git commit -m "feat(dupes): size-grouping stage

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

---

### Task 2: `dupes` — 해시 유틸 (부분 + 전체)

**Files:**
- Modify: `src-tauri/src/dupes.rs`, `src-tauri/Cargo.toml` (`blake3`)
- Test: 같은 파일 tests

**Interfaces:**
- Consumes: 없음
- Produces:
  - `dupes::hash_prefix(path: &Path, prefix_len: usize) -> Result<String, String>` — 파일 앞 prefix_len 바이트의 blake3 hex. 파일이 더 작으면 있는 만큼만
  - `dupes::hash_full(path: &Path) -> Result<String, String>` — 전체 blake3 hex (스트리밍)

- [ ] **Step 1: 의존성 추가**

Run: `cd src-tauri; cargo add blake3`

- [ ] **Step 2: 실패하는 테스트 작성**

`tests` 모듈에 추가:

```rust
    use std::io::Write;

    fn write_file(dir: &std::path::Path, name: &str, bytes: &[u8]) -> PathBuf {
        let p = dir.join(name);
        let mut f = std::fs::File::create(&p).unwrap();
        f.write_all(bytes).unwrap();
        p
    }

    #[test]
    fn prefix_hash_same_head_matches_regardless_of_tail() {
        let tmp = tempfile::tempdir().unwrap();
        let a = write_file(tmp.path(), "a.bin", b"HEADHEADHEAD-tailA");
        let b = write_file(tmp.path(), "b.bin", b"HEADHEADHEAD-tailB");
        // 앞 12바이트만 보면 같음
        assert_eq!(hash_prefix(&a, 12).unwrap(), hash_prefix(&b, 12).unwrap());
        // 전체는 다름
        assert_ne!(hash_full(&a).unwrap(), hash_full(&b).unwrap());
    }

    #[test]
    fn full_hash_identical_content_matches() {
        let tmp = tempfile::tempdir().unwrap();
        let a = write_file(tmp.path(), "a.bin", b"identical bytes here");
        let b = write_file(tmp.path(), "b.bin", b"identical bytes here");
        assert_eq!(hash_full(&a).unwrap(), hash_full(&b).unwrap());
    }

    #[test]
    fn hash_prefix_shorter_file_uses_available_bytes() {
        let tmp = tempfile::tempdir().unwrap();
        let a = write_file(tmp.path(), "a.bin", b"tiny");
        // prefix_len이 파일보다 커도 성공 (있는 4바이트만)
        assert!(hash_prefix(&a, 4096).is_ok());
    }

    #[test]
    fn hash_missing_file_is_err() {
        let tmp = tempfile::tempdir().unwrap();
        assert!(hash_full(&tmp.path().join("ghost")).is_err());
        assert!(hash_prefix(&tmp.path().join("ghost"), 16).is_err());
    }
```

- [ ] **Step 3: 실패 확인**

Run: `cd src-tauri; cargo test dupes`
Expected: COMPILE ERROR — `hash_prefix`/`hash_full` not found

- [ ] **Step 4: 구현**

`dupes.rs`에 추가 (imports에 `use std::path::Path;` `use std::io::Read;` 필요):

```rust
/// 2단계: 앞 prefix_len 바이트만 해시 — 대용량 파일의 전체 해시를 피하는 저비용 필터
pub fn hash_prefix(path: &Path, prefix_len: usize) -> Result<String, String> {
    let mut f = std::fs::File::open(path).map_err(|e| e.to_string())?;
    let mut buf = vec![0u8; prefix_len];
    let mut filled = 0;
    // 짧은 read를 대비해 EOF까지 채운다
    loop {
        let n = f.read(&mut buf[filled..]).map_err(|e| e.to_string())?;
        if n == 0 {
            break;
        }
        filled += n;
        if filled == prefix_len {
            break;
        }
    }
    Ok(blake3::hash(&buf[..filled]).to_hex().to_string())
}

/// 3단계: 전체 스트리밍 해시 — 부분 해시가 충돌한 후보만 여기 도달
pub fn hash_full(path: &Path) -> Result<String, String> {
    let mut f = std::fs::File::open(path).map_err(|e| e.to_string())?;
    let mut hasher = blake3::Hasher::new();
    let mut buf = [0u8; 65536];
    loop {
        let n = f.read(&mut buf).map_err(|e| e.to_string())?;
        if n == 0 {
            break;
        }
        hasher.update(&buf[..n]);
    }
    Ok(hasher.finalize().to_hex().to_string())
}
```

- [ ] **Step 5: 통과 확인 + Commit**

Run: `cd src-tauri; cargo test dupes`
Expected: 7 tests PASS

```powershell
git add src-tauri
git commit -m "feat(dupes): blake3 prefix and full hashing

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

---

### Task 3: `dupes` — 3단계 파이프라인 확정

**Files:**
- Modify: `src-tauri/src/dupes.rs`
- Test: 같은 파일 tests

**Interfaces:**
- Consumes: Task 1-2 전부
- Produces:
  - `dupes::DupeGroup { hash: String, size: u64, paths: Vec<String> }` (serde::Serialize)
  - `dupes::find_duplicates(files: Vec<FileEntry>, prefix_len: usize) -> Vec<DupeGroup>` — size→prefix→full 3단계, 최종 그룹은 총 낭비 용량(size*(count-1)) 내림차순. 해시 실패한 파일은 조용히 건너뜀(그룹에서 빠짐)

- [ ] **Step 1: 실패하는 테스트 작성**

`tests` 모듈에 추가:

```rust
    #[test]
    fn end_to_end_finds_true_duplicates_only() {
        let tmp = tempfile::tempdir().unwrap();
        // dup1/dup2: 완전 동일. near1/near2: 같은 크기+같은 앞부분, 다른 꼬리 → 전체해시서 갈림.
        let d1 = write_file(tmp.path(), "d1", b"AAAABBBBCCCCDDDD");
        let d2 = write_file(tmp.path(), "d2", b"AAAABBBBCCCCDDDD");
        let n1 = write_file(tmp.path(), "n1", b"AAAABBBBCCCCXXX1");
        let n2 = write_file(tmp.path(), "n2", b"AAAABBBBCCCCXXX2");
        let solo = write_file(tmp.path(), "solo", b"different length entirely");

        let files = vec![
            FileEntry { path: d1, size: 16 },
            FileEntry { path: d2, size: 16 },
            FileEntry { path: n1, size: 16 },
            FileEntry { path: n2, size: 16 },
            FileEntry { path: solo.clone(), size: std::fs::metadata(&solo).unwrap().len() },
        ];
        let groups = find_duplicates(files, 8);

        // d1/d2만 진짜 중복. n1/n2는 전체해시서 갈리고, solo는 크기 단독.
        assert_eq!(groups.len(), 1);
        assert_eq!(groups[0].paths.len(), 2);
        assert_eq!(groups[0].size, 16);
        let names: Vec<String> = groups[0]
            .paths
            .iter()
            .map(|p| std::path::Path::new(p).file_name().unwrap().to_string_lossy().into_owned())
            .collect();
        assert!(names.contains(&"d1".to_string()) && names.contains(&"d2".to_string()));
    }

    #[test]
    fn groups_sorted_by_wasted_space_desc() {
        let tmp = tempfile::tempdir().unwrap();
        // 작은-쌍(10B x2 = 낭비 10) vs 큰-쌍(1000B x2 = 낭비 1000)
        let big = vec![0u8; 1000];
        let b1 = write_file(tmp.path(), "b1", &big);
        let b2 = write_file(tmp.path(), "b2", &big);
        let s1 = write_file(tmp.path(), "s1", b"tenbytes!!");
        let s2 = write_file(tmp.path(), "s2", b"tenbytes!!");
        let files = vec![
            FileEntry { path: b1, size: 1000 },
            FileEntry { path: b2, size: 1000 },
            FileEntry { path: s1, size: 10 },
            FileEntry { path: s2, size: 10 },
        ];
        let groups = find_duplicates(files, 4096);
        assert_eq!(groups.len(), 2);
        assert_eq!(groups[0].size, 1000); // 큰 낭비가 먼저
    }

    #[test]
    fn hash_failures_are_skipped_not_fatal() {
        let tmp = tempfile::tempdir().unwrap();
        let d1 = write_file(tmp.path(), "d1", b"same content x");
        let d2 = write_file(tmp.path(), "d2", b"same content x");
        let files = vec![
            FileEntry { path: d1, size: 14 },
            FileEntry { path: d2, size: 14 },
            FileEntry { path: tmp.path().join("ghost"), size: 14 }, // 존재하지 않음
        ];
        // ghost는 크기 그룹엔 들어가지만 해시 단계서 실패 → 조용히 빠지고 d1/d2는 확정
        let groups = find_duplicates(files, 4096);
        assert_eq!(groups.len(), 1);
        assert_eq!(groups[0].paths.len(), 2);
    }
```

- [ ] **Step 2: 실패 확인**

Run: `cd src-tauri; cargo test dupes`
Expected: COMPILE ERROR — `find_duplicates`/`DupeGroup` not found

- [ ] **Step 3: 구현**

`dupes.rs`에 추가:

```rust
#[derive(Debug, Clone, serde::Serialize)]
pub struct DupeGroup {
    pub hash: String,
    pub size: u64,
    pub paths: Vec<String>,
}

/// 같은 (size, hash) 파일을 그룹으로 묶는 헬퍼. 해시 실패 파일은 제외.
fn regroup_by_hash(
    group: Vec<FileEntry>,
    hash_fn: impl Fn(&Path) -> Result<String, String>,
) -> HashMap<String, Vec<FileEntry>> {
    let mut by_hash: HashMap<String, Vec<FileEntry>> = HashMap::new();
    for f in group {
        let Ok(h) = hash_fn(&f.path) else { continue };
        by_hash.entry(h).or_default().push(f);
    }
    by_hash
}

/// 전체 3단계 파이프라인. 최종 그룹은 낭비 용량(size*(n-1)) 내림차순.
pub fn find_duplicates(files: Vec<FileEntry>, prefix_len: usize) -> Vec<DupeGroup> {
    let mut out: Vec<DupeGroup> = Vec::new();
    for size_group in group_by_size(files) {
        let size = size_group[0].size;
        // 2단계: 부분 해시로 재그룹, 2개 이상만
        for (_, prefix_group) in regroup_by_hash(size_group, |p| hash_prefix(p, prefix_len)) {
            if prefix_group.len() < 2 {
                continue;
            }
            // 3단계: 전체 해시로 확정, 2개 이상만
            for (hash, full_group) in regroup_by_hash(prefix_group, hash_full) {
                if full_group.len() < 2 {
                    continue;
                }
                out.push(DupeGroup {
                    hash,
                    size,
                    paths: full_group
                        .iter()
                        .map(|f| f.path.to_string_lossy().into_owned())
                        .collect(),
                });
            }
        }
    }
    // 낭비 용량 내림차순
    out.sort_by(|a, b| {
        let wa = a.size * (a.paths.len() as u64 - 1);
        let wb = b.size * (b.paths.len() as u64 - 1);
        wb.cmp(&wa)
    });
    out
}
```

- [ ] **Step 4: 통과 확인 + Commit**

Run: `cd src-tauri; cargo test dupes`
Expected: 10 tests PASS

```powershell
git add src-tauri
git commit -m "feat(dupes): three-stage duplicate pipeline

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

---

### Task 4: 파일 수집 + 커맨드 계층

**Files:**
- Modify: `src-tauri/src/dupes.rs` (수집기), `src-tauri/src/commands.rs`, `src-tauri/src/lib.rs`
- Test: dupes.rs tests (수집기), commands.rs tests (순수 조율 함수 있으면)

**Interfaces:**
- Consumes: Task 1-3 전부, `scanner::keep_entry`(pub(crate)), `safety::trash_delete`
- Produces:
  - `dupes::collect_files(root: &Path) -> Vec<FileEntry>` — jwalk로 파일만 수집(심링크 제외, keep_entry 재사용), 크기 포함
  - command `find_duplicate_files(root: String) -> Result<Vec<DupeGroup>, String>` (`#[cfg(not(coverage))]`) — collect_files → find_duplicates(prefix_len=4096)
  - `clean_paths`(M2 기존) 재사용 — 중복 삭제는 별도 커맨드 없이 기존 정리 IPC로. 새 커맨드 없음

- [ ] **Step 1: 수집기 테스트 작성**

`dupes.rs` tests에 추가:

```rust
    #[cfg(unix)]
    #[test]
    fn collect_files_excludes_dirs_and_symlinks() {
        let tmp = tempfile::tempdir().unwrap();
        let root = tmp.path();
        write_file(root, "real.bin", b"data");
        std::fs::create_dir(root.join("sub")).unwrap();
        write_file(&root.join("sub"), "nested.bin", b"more");
        std::os::unix::fs::symlink(root.join("real.bin"), root.join("link.bin")).unwrap();

        let files = collect_files(root);
        let names: Vec<String> = files
            .iter()
            .map(|f| f.path.file_name().unwrap().to_string_lossy().into_owned())
            .collect();
        assert!(names.contains(&"real.bin".to_string()));
        assert!(names.contains(&"nested.bin".to_string()));
        assert!(!names.contains(&"link.bin".to_string()), "심링크 제외");
    }
```

- [ ] **Step 2: 실패 확인**

Run: `cd src-tauri; cargo test dupes`
Expected: Windows에서는 cfg(unix) 테스트 제외 컴파일 확인 → `collect_files` 없어 COMPILE ERROR

- [ ] **Step 3: 수집기 구현**

`dupes.rs`에 추가 (`use std::path::Path;` 있음):

```rust
/// jwalk로 파일만 수집 — 심링크/reparse는 scanner::keep_entry가 순회에서 제외
pub fn collect_files(root: &Path) -> Vec<FileEntry> {
    let mut out = Vec::new();
    let walker = jwalk::WalkDir::new(root)
        .follow_links(false)
        .skip_hidden(false)
        .process_read_dir(|_d, _p, _s, children| {
            children.retain(|r| r.as_ref().map(crate::scanner::keep_entry).unwrap_or(true));
        });
    for entry in walker {
        let Ok(e) = entry else { continue };
        if !e.file_type().is_file() {
            continue;
        }
        let Ok(md) = e.metadata() else { continue };
        out.push(FileEntry { path: e.path(), size: md.len() });
    }
    out
}
```

- [ ] **Step 4: 커맨드 구현**

`commands.rs`에 추가 (`use crate::dupes;`는 `#[cfg(not(coverage))]` — 순수 로직이 dupes를 직접 쓰지 않으면):

```rust
#[cfg(not(coverage))]
#[tauri::command]
pub fn find_duplicate_files(root: String) -> Result<Vec<dupes::DupeGroup>, String> {
    let files = dupes::collect_files(std::path::Path::new(&root));
    Ok(dupes::find_duplicates(files, 4096))
}
```

`lib.rs` invoke_handler에 `commands::find_duplicate_files` 추가.

- [ ] **Step 5: 전체 테스트 + 양쪽 check**

Run: `cd src-tauri; cargo test`
Expected: 기존 + 신규 전부 PASS (unix 수집기 테스트는 Windows서 제외)

Run (bash): `RUSTFLAGS="--cfg coverage" cargo check`
Expected: 경고 0

- [ ] **Step 6: Commit**

```powershell
git add src-tauri
git commit -m "feat(dupes): file collection and duplicate-finding command

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

---

### Task 5: 프론트엔드 — 중복 뷰

**Files:**
- Create: `src/lib/Duplicates.svelte`
- Modify: `src/lib/api.ts`, `src/routes/+page.svelte`

**Interfaces:**
- Consumes: Task 4 커맨드, 기존 `cleanPaths`, `fmtBytes`
- Produces: api.ts `findDuplicateFiles(root)` + 타입 `DupeGroup`; `Duplicates.svelte` props `{ scannedRoot: string | null }`

- [ ] **Step 1: api.ts 추가**

`src/lib/api.ts`에 추가:

```typescript
export interface DupeGroup {
  hash: string;
  size: number;
  paths: string[];
}

export const findDuplicateFiles = (root: string) =>
  invoke<DupeGroup[]>("find_duplicate_files", { root });
```

- [ ] **Step 2: Duplicates.svelte 작성**

`src/lib/Duplicates.svelte`:

```svelte
<script lang="ts">
  import * as api from "./api";
  import { fmtBytes } from "./fmt";

  let { scannedRoot }: { scannedRoot: string | null } = $props();

  let groups: api.DupeGroup[] = $state([]);
  let busy = $state(false);
  let loadError = $state("");
  // 각 그룹에서 삭제 대상으로 선택된 경로 (보존할 하나를 제외한 나머지)
  let toDelete: Set<string> = $state(new Set());
  let results: api.CleanResult[] = $state([]);

  async function scan() {
    if (!scannedRoot) return;
    busy = true;
    loadError = "";
    results = [];
    try {
      groups = await api.findDuplicateFiles(scannedRoot);
      // 기본 선택: 각 그룹의 첫 파일을 보존, 나머지를 삭제 후보로
      const next = new Set<string>();
      for (const g of groups) {
        for (const p of g.paths.slice(1)) next.add(p);
      }
      toDelete = next;
    } catch (e) {
      loadError = String(e);
    } finally {
      busy = false;
    }
  }

  function toggle(path: string) {
    const next = new Set(toDelete);
    next.has(path) ? next.delete(path) : next.add(path);
    toDelete = next;
  }

  let reclaimable = $derived(
    groups.reduce(
      (sum, g) => sum + g.size * g.paths.filter((p) => toDelete.has(p)).length,
      0,
    ),
  );

  async function deleteSelected() {
    const paths = [...toDelete];
    if (paths.length === 0) return;
    // 안전: 그룹 전체가 삭제 선택되면 최소 1개는 보존하도록 막는다
    for (const g of groups) {
      const remaining = g.paths.filter((p) => !toDelete.has(p));
      if (remaining.length === 0) {
        alert(`중복 그룹 하나가 통째로 삭제 선택됐습니다. 각 그룹에서 최소 1개는 보존해야 합니다.`);
        return;
      }
    }
    const okay = confirm(
      `${paths.length}개 중복 파일을 휴지통으로 보냅니다 (${fmtBytes(reclaimable)} 확보).\n` +
        `각 그룹의 사본 1개는 보존됩니다. 휴지통에서 복원할 수 있습니다.`,
    );
    if (!okay) return;
    busy = true;
    try {
      results = await api.cleanPaths(paths);
      await scan();
    } catch (e) {
      loadError = String(e);
    } finally {
      busy = false;
    }
  }
</script>

<section>
  <h2>
    중복 파일 {scannedRoot ? "" : "(먼저 스캔하세요)"}
    <button onclick={scan} disabled={busy || !scannedRoot}>{busy ? "찾는 중…" : "중복 찾기"}</button>
  </h2>
  {#if loadError}<p class="error">{loadError}</p>{/if}

  {#if groups.length === 0 && !busy}
    <p class="muted">중복을 찾으려면 스캔 후 "중복 찾기"를 누르세요.</p>
  {/if}

  {#each groups as g (g.hash)}
    <div class="group">
      <div class="ghead">
        {g.paths.length}개 사본 · 각 {fmtBytes(g.size)} · 낭비 {fmtBytes(g.size * (g.paths.length - 1))}
      </div>
      <ul>
        {#each g.paths as p (p)}
          <li>
            <label>
              <input
                type="checkbox"
                disabled={busy}
                checked={toDelete.has(p)}
                onchange={() => toggle(p)}
              />
              <span class="path" title={p}>{p}</span>
              {#if !toDelete.has(p)}<em class="keep">보존</em>{/if}
            </label>
          </li>
        {/each}
      </ul>
    </div>
  {/each}

  {#if groups.length > 0}
    <div class="actions">
      <button onclick={deleteSelected} disabled={busy || toDelete.size === 0}>
        선택 중복 휴지통으로 ({fmtBytes(reclaimable)})
      </button>
    </div>
  {/if}

  {#if results.length > 0}
    <p>{results.filter((r) => r.ok).length}/{results.length}개 휴지통으로 이동 — 복원 가능합니다.</p>
  {/if}
</section>

<style>
  section { margin-top: 1.5rem; border-top: 1px solid #ddd; padding-top: 1rem; }
  h2 { display: flex; gap: 0.75rem; align-items: center; }
  .group { border: 1px solid #eee; border-radius: 4px; margin: 0.5rem 0; padding: 0.5rem; }
  .ghead { font-size: 0.85rem; color: #555; margin-bottom: 0.25rem; }
  .group ul { list-style: none; padding: 0; margin: 0; }
  .group li { padding: 1px 0; }
  .path { overflow-wrap: anywhere; }
  .keep { color: #080; margin-left: 0.5rem; font-size: 0.8rem; }
  .muted { color: #999; }
  .error { color: #b00; }
</style>
```

- [ ] **Step 3: +page.svelte 연결**

script에 추가:

```typescript
  import Duplicates from "$lib/Duplicates.svelte";
```

`<Cleanup scannedRoot={...} />` 바로 아래에 추가:

```svelte
  <Duplicates scannedRoot={crumbs.length > 0 ? crumbs[0] : null} />
```

- [ ] **Step 4: 게이트**

Run: `npm run build`, `npm run check`, `npm test`, `npm run coverage`
Expected: 모두 클린, JS 커버리지 100 유지 (Duplicates.svelte는 순수 .ts 아님 — vitest.config.ts 변경 없음)

- [ ] **Step 5: Commit**

```powershell
git add src
git commit -m "feat(ui): duplicate-files review panel with keep-one safeguard

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

---

### Task 6: 커버리지 정합 + 최종 검증 + PR

**Files:** 필요 시 테스트 추가

**Interfaces:**
- Consumes: Task 1-5 전체
- Produces: 리눅스 게이트 100% 라인 커버리지 + M3 PR

- [ ] **Step 1: 로컬 측정**

Run (bash, src-tauri): `cargo llvm-cov --all-features --fail-under-lines 100 --show-missing-lines`

- [ ] **Step 2: 갭 분석 및 폐쇄**

허용 미커버: cfg(unix)-only 테스트가 커버하는 arm(리눅스서 실행), cfg(windows)-only 경로. 그 외는 테스트 추가로 닫는다. 특히:
- `hash_prefix`의 짧은-read 루프 `if filled == prefix_len { break }` 양쪽 분기 — 기존 테스트가 이미 커버(짧은 파일/긴 prefix, 그리고 정확히 채우는 경우). 미커버면 정확히 prefix_len인 파일 테스트 추가
- `regroup_by_hash`의 `Err(_) => continue` — `hash_failures_are_skipped_not_fatal`가 커버
- `collect_files`의 metadata 실패 arm — cfg(unix)로 권한 없는 파일 or 존재-사라진 경로 폴트 주입, 어려우면 리눅스 게이트 카테고리(a)로 분류하고 보고
- `find_duplicate_files` 커맨드는 `#[cfg(not(coverage))]`이라 게이트서 사라짐 — 재확인 `RUSTFLAGS="--cfg coverage" cargo check`

- [ ] **Step 3: 전체 게이트**

Run: `cd src-tauri; cargo test` → 전체 PASS
Run: `npm test; npm run coverage; npm run build; npm run check` → 클린
Run (bash): `RUSTFLAGS="--cfg coverage" cargo check` → 경고 0

- [ ] **Step 4: Commit (커버리지 보강분 있으면)**

```powershell
git add src-tauri
git commit -m "test(coverage): close m3 line-coverage gaps for the linux gate

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

- [ ] **Step 5: 푸시 + PR**

```powershell
git push -u origin feat/m3-dupes
gh pr create --repo ContextualWisdomLab/disksage --base main --head feat/m3-dupes --title "feat: M3 duplicate-file detection" --body "M3 milestone per docs/superpowers/specs/2026-07-10-disksage-design.md: three-stage duplicate pipeline (size grouping -> blake3 prefix hash -> blake3 full hash), file collection excluding symlinks, review UI with a keep-one-per-group safeguard. Deletion reuses the M2 safety layer (trash-only, journaled) — no new delete code path.

🤖 Generated with [Claude Code](https://claude.com/claude-code)"
```

Expected: PR URL. 이후 조직 파이프라인 (M1/M2 확립 패턴: linguist-generated로 리뷰봇 통과, 스테일 리뷰 dismiss, 전 체크 그린+APPROVED+CLEAN이면 `gh pr merge --auto --squash`).

- [ ] **Step 6: 사람 검증 체크리스트 (사용자 전달용)**

1. `npm run tauri dev` → 스캔 → "중복 찾기" → 같은 내용 파일 2벌 만든 폴더에서 그룹 표시 확인
2. 그룹 내 기본 "보존 1 + 나머지 삭제" 선택 확인, 체크 토글
3. 그룹 전체 삭제 시도 → "최소 1개 보존" 경고 확인
4. 실행 → 휴지통 확인 → 복원
